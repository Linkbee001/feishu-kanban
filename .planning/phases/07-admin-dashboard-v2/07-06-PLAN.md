---
phase: 07-admin-dashboard-v2
plan: 06
type: execute
wave: 6
depends_on: [07-04, 07-05]
files_modified:
  - frontend/src/pages/DashboardPage.tsx
  - frontend/src/pages/SettingsPage.tsx
  - src/modules/admin/admin.controller.ts
  - src/modules/admin/admin.service.ts
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - REQ-07-06
must_haves:
  truths:
    - Dashboard page shows stats cards (total groups, active sessions, today's messages)
    - Dashboard shows recent activity list
    - Dashboard has quick action buttons
    - Settings page displays configuration form
    - Settings can be saved and persisted
    - Navigation works between all pages
    - Mobile responsive (sidebar collapsible)
    - All pages have proper empty states
  artifacts:
    - path: frontend/src/pages/DashboardPage.tsx
      provides: Dashboard with stats and activity
      exports: [DashboardPage]
    - path: frontend/src/pages/SettingsPage.tsx
      provides: Settings configuration page
      exports: [SettingsPage]
  key_links:
    - from: Dashboard quick actions
      to: other pages
      via: navigation with router
    - from: Settings form
      to: backend API
      via: useApi hook
---

<objective>
Complete Dashboard and Settings pages, add polish and mobile responsiveness. Final wave integrates all features and ensures consistent UX across the admin interface.

Purpose: Provide overview dashboard and system configuration, finalize the admin interface.
Output: Complete DashboardPage, SettingsPage, mobile responsive layout, final polish.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-admin-dashboard-v2/07-CONTEXT.md
@.planning/phases/07-admin-dashboard-v2/07-UI-SPEC.md

## UI-SPEC Dashboard Requirements

Layout:
```
┌──────────────────────────────────────────────────────────────┐
│ Dashboard                                                    │
│ 系统概览与统计信息                                            │
├──────────────────────────────────────────────────────────────┤
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│ │ 总群数       │ │ 活跃会话     │ │ 今日消息     │          │
│ │ 156          │ │ 42           │ │ 1,234        │          │
│ └──────────────┘ └──────────────┘ └──────────────┘          │
├──────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────┐   │
│ │ 最近活动                                                │   │
│ │ [图标] 群 "Project A" 配置更新        10分钟前         │   │
│ │ [图标] 任务执行成功                    32分钟前         │   │
│ └────────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────┐   │
│ │ 快速操作                                                │   │
│ │ [配置新群] [查看消息] [查看运行日志]                    │   │
│ └────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

Stats Cards:
- Icon + Title + Large number (text-[28px] font-bold)
- Subtitle with trend indicator
- Hover: subtle shadow
- Background: bg-white, rounded-2xl, p-6

## UI-SPEC Settings Requirements

Layout:
```
┌──────────────────────────────────────────────────────────────┐
│ Settings                                                     │
│ 系统配置                                                    │
├──────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────┐   │
│ │ 通用设置                                                │   │
│ │ 默认模型 [Dropdown]                                    │   │
│ │ 自动同步群信息 [Toggle]                                │   │
│ │ 消息保留天数 [Number input]                            │   │
│ └────────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────┐   │
│ │ 通知设置                                                │   │
│ │ 启用邮件通知 [Toggle]                                  │   │
│ │ 通知邮箱 [Input]                                       │   │
│ └────────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│                              [保存设置]                      │
└──────────────────────────────────────────────────────────────┘
```

## Mobile Responsive Requirements

Per UI-SPEC Responsive Behavior:
- Sidebar collapsible on tablet/mobile (< 1024px)
- Hamburger menu button in header
- Full-screen sidebar overlay when open
- Swipe to close (optional)
- Data table horizontal scroll
- Drawer becomes full-screen on mobile

## Pitfalls from RESEARCH.md

Pitfall 5: Sidebar Mobile Responsiveness
- Implement collapsible sidebar using state
- Use breakpoints (Tailwind lg:, md:)
- Test on different screen sizes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Backend Dashboard Stats API</name>
  <files>
    - src/modules/admin/admin.controller.ts
    - src/modules/admin/admin.service.ts
  </files>
  <action>
Add dashboard stats endpoint.

New endpoint:
```typescript
@Get('api/admin/dashboard/stats')
getDashboardStats() {
  return this.admin.getDashboardStats();
}

@Get('api/admin/dashboard/activity')
getRecentActivity(@Query('limit') limit?: string) {
  return this.admin.getRecentActivity(limit ? parseInt(limit, 10) : 10);
}
```

Stats to include:
- totalGroups: count of GroupAgentSession
- activeSessions: count where sessionMode === 'active'
- pendingConfig: count where sessionMode === 'pending_config'
- todayMessages: count of MessageSource since today midnight
- totalRuns: count of AgentRun

Recent activity:
- Query recent RuntimeEvent, Project updates, GroupAgentSession changes
- Limit to last 10 events
- Format: { type, description, timestamp, link? }

Response shapes:
```typescript
interface DashboardStats {
  totalGroups: number;
  activeSessions: number;
  pendingConfig: number;
  todayMessages: number;
  totalRuns: number;
}

interface ActivityItem {
  id: string;
  type: 'config_update' | 'task_complete' | 'group_bind' | 'message_received';
  title: string;
  description: string;
  timestamp: string;
  link?: string;
}
```
  </action>
  <verify>
    <automated>grep -c "dashboard/stats" C:/workspace/feishu-kanban/src/modules/admin/admin.controller.ts && grep -c "getDashboardStats" C:/workspace/feishu-kanban/src/modules/admin/admin.service.ts</automated>
  </verify>
  <done>Dashboard stats and activity endpoints implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement DashboardPage</name>
  <files>frontend/src/pages/DashboardPage.tsx</files>
  <action>
Build DashboardPage with stats cards and activity feed.

Components needed:
1. StatsCard component (or inline):
   - Icon (Lucide: Users, Activity, MessageSquare)
   - Title: text-sm text-muted
   - Value: text-[28px] font-bold text-ink
   - Subtitle: text-xs with trend
   - Background: bg-white rounded-2xl p-6 shadow-sm

2. Stats grid:
   - 3 columns on desktop
   - 1 column on mobile
   - Grid: grid-cols-1 md:grid-cols-3 gap-6

3. Recent Activity section:
   - List of activity items
   - Icon per activity type
   - Title + timestamp
   - Clickable links

4. Quick Actions section:
   - Button: "配置新群" → navigate to /admin/groups
   - Button: "查看消息" → navigate to /admin/messages
   - Button: "查看运行日志" → navigate to /admin/runs
   - Styled as primary/outline buttons

Data fetching:
- Use useApi or custom hook for stats
- Fetch on mount
- Refresh button

Icons mapping:
- Users: Users
- Active sessions: Activity
- Messages: MessageSquare

Activity types:
- Config update: Settings icon
- Task complete: CheckCircle icon
- Group bind: Users icon
- Message received: MessageSquare icon
  </action>
  <verify>
    <automated>grep -c "StatsCard\|stats" C:/workspace/feishu-kanban/frontend/src/pages/DashboardPage.tsx && grep -c "quick.*action\|配置新群" C:/workspace/feishu-kanban/frontend/src/pages/DashboardPage.tsx</automated>
  </verify>
  <done>DashboardPage with stats cards, activity feed, and quick actions</done>
</task>

<task type="auto">
  <name>Task 3: Create Settings Form and Backend</name>
  <files>
    - src/modules/admin/admin.controller.ts
    - src/modules/admin/admin.service.ts
    - frontend/src/pages/SettingsPage.tsx
  </files>
  <action>
Build SettingsPage with configuration form.

Backend endpoints:
```typescript
@Get('api/admin/settings')
getSettings() {
  return this.admin.getSettings();
}

@Post('api/admin/settings')
updateSettings(@Body() settings: SettingsDto) {
  return this.admin.updateSettings(settings);
}
```

Settings model (store in database or config):
- defaultModel: string
- autoSyncGroups: boolean
- messageRetentionDays: number
- emailNotifications: boolean
- notificationEmail: string

Frontend SettingsPage:
1. Header:
   - Title: "系统设置"
   - Subtitle: "系统配置"

2. Form sections:

   通用设置 section:
   - Default model select (dropdown with available models)
   - Auto-sync groups toggle (switch)
   - Message retention days (number input)

   通知设置 section:
   - Enable email notifications toggle
   - Notification email input (shown when enabled)

3. Form handling:
   - Load settings on mount
   - Track dirty state
   - Save button submits to API
   - Success/error toast

4. Validation:
   - Email format if notifications enabled
   - Retention days > 0

Styling per UI-SPEC:
- Sections: bg-white rounded-2xl p-6 mb-6
- Section titles: text-lg font-semibold mb-4
- Form fields: space-y-4
- Save button: primary style, right-aligned
  </action>
  <verify>
    <automated>grep -c "api/admin/settings" C:/workspace/feishu-kanban/src/modules/admin/admin.controller.ts && grep -c "SettingsPage\|系统设置" C:/workspace/feishu-kanban/frontend/src/pages/SettingsPage.tsx</automated>
  </verify>
  <done>SettingsPage with form sections and backend persistence</done>
</task>

<task type="auto">
  <name>Task 4: Add Mobile Responsiveness to Layout</name>
  <files>
    - frontend/src/components/layout/Layout.tsx
    - frontend/src/components/layout/Sidebar.tsx
  </files>
  <action>
Implement mobile responsive sidebar per UI-SPEC and Pitfall 5.

Changes to Layout:
1. Add mobile state:
   - const [sidebarOpen, setSidebarOpen] = useState(false);
   - const isMobile = useMediaQuery('(max-width: 1024px)');

2. Header modification:
   - Add hamburger menu button on mobile (left side)
   - Menu icon: Menu from Lucide

3. Sidebar positioning:
   - Desktop: fixed left, always visible
   - Mobile: fixed left, hidden by default, overlay when open
   - Animation: slide in from left

4. Backdrop:
   - On mobile, clicking backdrop closes sidebar
   - Semi-transparent overlay

Changes to Sidebar:
1. Accept isOpen and onClose props
2. Close button inside sidebar for mobile
3. Swipe gesture support (optional)

Implementation approach:
```typescript
// Mobile detection hook
function useMediaQuery(query: string) {
  const [matches, setMatches] = useState(false);
  useEffect(() => {
    const media = window.matchMedia(query);
    setMatches(media.matches);
    media.addEventListener('change', e => setMatches(e.matches));
    return () => media.removeEventListener('change', ...);
  }, [query]);
  return matches;
}
```

Tailwind responsive classes:
- Sidebar: hidden lg:block (desktop), fixed inset-0 z-50 (mobile overlay)
- Content: lg:ml-[280px] (space for sidebar on desktop)

Address Pitfall 5 from RESEARCH.md.
  </action>
  <verify>
    <automated>grep -c "sidebarOpen\|isMobile" C:/workspace/feishu-kanban/frontend/src/components/layout/Layout.tsx && grep -c "Menu" C:/workspace/feishu-kanban/frontend/src/components/layout/Layout.tsx</automated>
  </verify>
  <done>Mobile responsive layout with collapsible sidebar and hamburger menu</done>
</task>

<task type="auto">
  <name>Task 5: Add Empty States and Error Handling</name>
  <files>
    - frontend/src/pages/DashboardPage.tsx
    - frontend/src/pages/GroupsPage.tsx
    - frontend/src/pages/MessagesPage.tsx
    - frontend/src/pages/RunsPage.tsx
  </files>
  <action>
Ensure all pages have proper empty states and error handling.

Per UI-SPEC Empty States:
| Page | Heading | Body |
|------|---------|------|
| Groups | 暂无群数据 | 点击「配置新群」添加第一个群 |
| Messages | 暂无消息记录 | 选择群聊查看消息历史 |
| Runs | 暂无运行日志 | 等待任务执行后查看日志 |
| Dashboard | 欢迎使用 | 系统运行正常，暂无统计数据 |

Empty state component:
```typescript
interface EmptyStateProps {
  icon: React.ComponentType;
  heading: string;
  body: string;
  action?: { label: string; onClick: () => void };
}
```

Error states per UI-SPEC:
- "加载失败，请刷新重试" for data loading errors
- Retry button

Add to each page:
1. GroupsPage: Empty when groups.length === 0
2. MessagesPage: Already has empty state (verify)
3. RunsPage: Already has empty state (verify)
4. DashboardPage: Empty when no stats/activity

Error boundaries:
- Wrap page content in error handling
- Show error message with retry
- Log error for debugging

Loading states:
- Skeleton screens for stats cards
- Loading spinner for tables
- "加载中..." text
  </action>
  <verify>
    <automated>grep -c "暂无群数据\|暂无消息记录\|暂无运行日志\|欢迎使用" C:/workspace/feishu-kanban/frontend/src/pages/*.tsx | wc -l</automated>
  </verify>
  <done>All pages have consistent empty states and error handling</done>
</task>

<task type="auto">
  <name>Task 6: Final Integration and Polish</name>
  <files>
    - frontend/src/App.tsx
    - frontend/src/components/layout/Sidebar.tsx
  </files>
  <action>
Final integration, navigation verification, and polish.

Tasks:
1. Verify all routes work:
   - Navigate to each page from sidebar
   - Verify URL updates correctly
   - Verify active state highlights correctly

2. Add breadcrumb navigation (optional):
   - Simple breadcrumbs below header
   - Dashboard / Groups / etc.

3. Update Sidebar if needed:
   - Ensure all nav items link correctly
   - Active state matches current route
   - Groups submenu expands for child routes

4. Page transitions:
   - Smooth fade or slide transitions between pages
   - Optional: Framer Motion or simple CSS

5. Scroll to top:
   - On route change, scroll content to top

6. Keyboard shortcuts (optional):
   - Ctrl/Cmd+K for search
   - ESC to close drawers/modals

7. Final styling check:
   - Colors match UI-SPEC (#1d6b57 primary, #f5efe6 panel)
   - Spacing uses 8-point grid
   - Typography: 28/20/14/12px sizes

8. Cleanup:
   - Remove any TODO comments
   - Ensure no console errors
   - Remove old/unused components

9. Build verification:
   - npm run build succeeds
   - No TypeScript errors
   - No ESLint warnings (or acceptable level)
  </action>
  <verify>
    <automated>cd C:/workspace/feishu-kanban/frontend && npm run build 2>&1 | grep -E "(error|warning)" | wc -l</automated>
  </verify>
  <done>Final integration complete, build succeeds, all navigation verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings API | AdminAuthGuard protects configuration |
| Dashboard data | Admin-only statistics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Elevation | Settings update | Mitigate | Verify AdminAuthGuard on POST endpoint |
| T-07-19 | DoS | Stats queries | Accept | Queries are simple counts, not heavy |
| T-07-20 | Info Disclosure | Dashboard stats | Accept | Aggregate data only, no sensitive details |
</threat_model>

<verification>
1. Dashboard displays stats cards with correct data
2. Recent activity list shows events
3. Quick action buttons navigate correctly
4. Settings form loads and saves
5. Mobile sidebar collapses and expands
6. Hamburger menu works on mobile
7. All pages have empty states
8. All pages have error handling
9. Build succeeds without errors
10. Navigation between all pages works smoothly
</verification>

<success_criteria>
- Dashboard complete with stats and activity
- Settings page functional with persistence
- Mobile responsive layout working
- Empty states on all pages
- No console errors
- Build succeeds
- All navigation functional
- UI matches UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-dashboard-v2/07-06-SUMMARY.md`
</output>
