---
phase: 07-admin-dashboard-v2
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - frontend/src/components/drawer/Drawer.tsx
  - frontend/src/components/drawer/GroupConfigDrawer.tsx
  - frontend/src/pages/GroupsPage.tsx
  - frontend/src/hooks/useGroupConfig.ts
autonomous: true
requirements:
  - REQ-07-04
must_haves:
  truths:
    - Clicking "配置" in Groups table opens right-side drawer
    - Drawer width is 480px per UI-SPEC
    - Drawer displays group config form with pre-filled data
    - Form has Sync button and Save Changes button
    - Saving updates config and refreshes table
    - Drawer closes on X, overlay click, or ESC
    - URL updates with ?drawer=group-config&chatId=xxx for persistence
  artifacts:
    - path: frontend/src/components/drawer/Drawer.tsx
      provides: Generic slide-over drawer component
      exports: [Drawer]
    - path: frontend/src/components/drawer/GroupConfigDrawer.tsx
      provides: Group configuration form in drawer
      exports: [GroupConfigDrawer]
    - path: frontend/src/pages/GroupsPage.tsx
      provides: Integrated drawer trigger and state
      exports: [GroupsPage]
  key_links:
    - from: GroupsPage row actions
      to: GroupConfigDrawer
      via: URL query params + drawer open state
    - from: GroupConfigDrawer
      to: GroupConfigService API
      via: useGroupConfig hook
    - from: Drawer
      to: Radix Dialog
      via: Dialog primitives for accessibility
---

<objective>
Implement slide-over drawer for group configuration editing per D-06. Drawer opens from right side, displays config form, allows editing and saving without leaving the groups page.

Purpose: Enable quick group configuration without context switch.
Output: Drawer component, GroupConfigDrawer with form, integration with GroupsPage.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-admin-dashboard-v2/07-CONTEXT.md
@.planning/phases/07-admin-dashboard-v2/07-UI-SPEC.md
@.planning/phases/07-admin-dashboard-v2/07-RESEARCH.md

## Existing Assets

From GroupConfigPage.tsx (existing):
- Form structure with sync flow
- useGroupSync and useCompleteConfig hooks
- Validation helpers (isRequired, isValidUrl, isValidChatId)
- Three sections: chat ID sync, auto-fill info, minimal form

From RESEARCH.md Pattern 3 (Drawer):
- Radix Dialog based implementation
- 480px width from right edge
- Backdrop blur and overlay click to close

## UI-SPEC Drawer Requirements

Specs:
- Width: 480px fixed
- Position: Right edge, slides from right
- Overlay: rgba(0,0,0,0.3) backdrop
- Animation: 300ms ease-out
- Close: X button, overlay click, ESC key

Content structure:
```
┌──────────────────────────────────────────────────┐
│ [X] 群配置编辑                                   │
├──────────────────────────────────────────────────┤
│ Step Indicator (同步 → 配置)                     │
│                                                  │
│ [Chat ID Input + Sync Button]                    │
│                                                  │
│ ──────────────────────────────────────────────── │
│                                                  │
│ [Form Fields]                                    │
│ • 项目名称 *                                     │
│ • 代码仓库                                       │
│ • 其他字段...                                    │
│                                                  │
│ ──────────────────────────────────────────────── │
│                                                  │
│ [Cancel]              [Save Changes]             │
└──────────────────────────────────────────────────┘
```

## Pitfalls from RESEARCH.md

Pitfall 1: Drawer State Synchronization
- Use URL query params for drawer state
- Sync drawer open with URL (?drawer=group-config&chatId=xxx)

Pitfall 4: Form State in Drawer
- Reset form when drawer opens for different group
- Use key prop or explicit reset
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Generic Drawer Component</name>
  <files>
    - frontend/src/components/drawer/Drawer.tsx
    - frontend/src/components/drawer/index.ts
  </files>
  <action>
Create reusable Drawer component using Radix Dialog.

Per RESEARCH.md Pattern 3 and UI-SPEC:

```typescript
interface DrawerProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  title: string;
  description?: string;
  children: React.ReactNode;
}
```

Implementation:
- Use @radix-ui/react-dialog primitives
- Fixed right positioning: fixed right-0 top-0 h-full
- Width: w-[480px] per UI-SPEC
- Backdrop: bg-black/30 backdrop-blur-sm
- Animation: transition-transform duration-300 ease-out
- Close button: X icon in header
- Header with title and close button
- Scrollable content area

Accessibility:
- Dialog.Title for screen readers
- ESC key handling (built into Radix)
- Focus trapping (built into Radix)

Create directory:
```bash
mkdir -p C:/workspace/feishu-kanban/frontend/src/components/drawer
```
  </action>
  <verify>
    <automated>grep -c "Dialog.Root" C:/workspace/feishu-kanban/frontend/src/components/drawer/Drawer.tsx && grep -c "w-\[480px\]" C:/workspace/feishu-kanban/frontend/src/components/drawer/Drawer.tsx</automated>
  </verify>
  <done>Drawer component created with Radix Dialog, 480px width, proper animations</done>
</task>

<task type="auto">
  <name>Task 2: Create GroupConfigDrawer Component</name>
  <files>frontend/src/components/drawer/GroupConfigDrawer.tsx</files>
  <action>
Create GroupConfigDrawer that wraps the config form in a drawer.

Props:
```typescript
interface GroupConfigDrawerProps {
  chatId: string | null;
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSaved?: () => void; // Callback to refresh table
}
```

Features:
1. Reuse form logic from existing GroupConfigPage:
   - Sync flow (chatId input + sync button)
   - Auto-filled group info display
   - Config form fields
   - Validation

2. Adapt for drawer context:
   - Pre-fill chatId from prop if provided
   - Skip chatId input step if already have group info
   - Show group info header
   - Form fields: projectName, repoUrl, repoBranch, modelName, mentionOnly

3. Integration:
   - Use existing useGroupSync and useCompleteConfig hooks
   - On save success: close drawer, call onSaved callback
   - Show success toast

4. Layout per UI-SPEC:
   - Header: "群配置编辑" + close button
   - Step indicator (同步 → 配置)
   - Form sections with dividers
   - Footer: Cancel + Save Changes buttons

Extract reusable form components if needed to avoid duplicating GroupConfigPage logic.
  </action>
  <verify>
    <automated>grep -c "GroupConfigDrawer" C:/workspace/feishu-kanban/frontend/src/components/drawer/GroupConfigDrawer.tsx && grep -c "useGroupSync\|useCompleteConfig" C:/workspace/feishu-kanban/frontend/src/components/drawer/GroupConfigDrawer.tsx</automated>
  </verify>
  <done>GroupConfigDrawer created with sync flow, form, validation, and save functionality</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Drawer with GroupsPage</name>
  <files>frontend/src/pages/GroupsPage.tsx</files>
  <action>
Update GroupsPage to integrate GroupConfigDrawer with URL-based state management.

Changes:
1. Add URL query param handling:
   - Read ?drawer=group-config&chatId=xxx on mount
   - Open drawer if params present
   - Update URL when drawer opens/closes

2. Row action handlers:
   - onConfigure(chatId): set URL params, open drawer
   - onViewMessages(chatId): navigate to /admin/messages?group=xxx
   - onUnbind(chatId): show confirm dialog (stub for now)

3. Drawer integration:
   - Import GroupConfigDrawer
   - Manage open state from URL params
   - Pass chatId from URL
   - onSaved callback: refetch groups table

4. URL sync pattern:
```typescript
const [searchParams, setSearchParams] = useSearchParams();
const drawerOpen = searchParams.get('drawer') === 'group-config';
const chatId = searchParams.get('chatId');

const openDrawer = (id: string) => {
  setSearchParams({ drawer: 'group-config', chatId: id });
};

const closeDrawer = () => {
  setSearchParams({}); // Clear params
};
```

5. Update row actions dropdown to call openDrawer.

This addresses Pitfall 1 from RESEARCH.md about drawer state synchronization.
  </action>
  <verify>
    <automated>grep -c "useSearchParams" C:/workspace/feishu-kanban/frontend/src/pages/GroupsPage.tsx && grep -c "GroupConfigDrawer" C:/workspace/feishu-kanban/frontend/src/pages/GroupsPage.tsx && grep -c "drawer=group-config" C:/workspace/feishu-kanban/frontend/src/pages/GroupsPage.tsx</automated>
  </verify>
  <done>GroupsPage updated with URL-based drawer state, row actions trigger drawer</done>
</task>

<task type="auto">
  <name>Task 4: Add Unbind Confirmation and API</name>
  <files>
    - src/modules/admin/admin.controller.ts
    - src/modules/admin/admin.service.ts
    - frontend/src/hooks/useGroups.ts
  </files>
  <action>
Add unbind group functionality (backend endpoint and frontend hook).

Backend (AdminController):
```typescript
@Post('api/admin/groups/:chatId/unbind')
async unbindGroup(@Param('chatId') chatId: string) {
  return this.admin.unbindGroup(chatId);
}
```

Backend (AdminService.unbindGroup):
- Find group session
- Call groupSessions.unbindProjectSession or similar
- Return { success: true }

Frontend (useGroups hook):
- Add unbind function
- POST to /api/admin/groups/${chatId}/unbind
- Return { unbind, unbinding, unbindError }

Update GroupsPage:
- Add ConfirmDialog for unbind action
- Show confirmation with group name
- On confirm: call unbind, then refetch table
- Show success/error toast

Reusable ConfirmDialog may already exist from previous phases - check and reuse if available.
  </action>
  <verify>
    <automated>grep -c "@Post.*groups.*unbind" C:/workspace/feishu-kanban/src/modules/admin/admin.controller.ts && grep -c "unbind" C:/workspace/feishu-kanban/frontend/src/hooks/useGroups.ts</automated>
  </verify>
  <done>Unbind endpoint implemented, frontend hook updated, GroupsPage has confirm dialog</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drawer form → API | AdminAuthGuard protects all endpoints |
| URL params | Client-side only, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Elevation | Unbind endpoint | Mitigate | Verify AdminAuthGuard on POST endpoint |
| T-07-09 | CSRF | Config update | Accept | Existing CSRF protection on backend |
| T-07-10 | XSS | Form display | Mitigate | React escapes HTML by default |
| T-07-11 | Info Disclosure | Drawer content | Accept | Admin-only data, no sensitive exposure |
</threat_model>

<verification>
1. Click "配置" opens drawer with 480px width from right
2. Drawer shows group config form
3. Sync flow works within drawer
4. Save updates config and refreshes table
5. Drawer closes on X, overlay click, ESC
6. URL updates with ?drawer=group-config&chatId=xxx
7. Refresh with URL opens drawer automatically
8. Unbind shows confirm dialog and works
</verification>

<success_criteria>
- Drawer opens/closes smoothly with 480px width
- Config form pre-fills with group data
- Save updates config and refreshes table
- URL state synchronization works
- Unbind with confirmation dialog functional
- All interactions per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-dashboard-v2/07-03-SUMMARY.md`
</output>
