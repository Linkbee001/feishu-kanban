---
phase: 08-admin-dashboard-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/src/components/ui/data-table.tsx
  - frontend/src/components/ui/data-table-pagination.tsx
  - frontend/src/components/ui/data-table-toolbar.tsx
  - frontend/src/components/ui/data-table-column-header.tsx
  - frontend/src/hooks/useGroups.ts
  - frontend/src/hooks/useMessages.ts
  - frontend/src/hooks/useRuns.ts
autonomous: true
requirements:
  - REQ-08-05
  - REQ-08-06
must_haves:
  truths:
    - "DataTable component accepts columns and data props"
    - "DataTable supports sorting via column headers"
    - "DataTable displays pagination controls"
    - "DataTable shows loading state with skeleton"
    - "DataTable handles empty state with message"
    - "Column header component supports sort indicators"
  artifacts:
    - path: "frontend/src/components/ui/data-table.tsx"
      provides: "Main DataTable component"
      exports: ["DataTable"]
      min_lines: 80
    - path: "frontend/src/components/ui/data-table-pagination.tsx"
      provides: "Pagination controls"
      exports: ["DataTablePagination"]
    - path: "frontend/src/components/ui/data-table-column-header.tsx"
      provides: "Sortable column header"
      exports: ["DataTableColumnHeader"]
    - path: "frontend/src/hooks/useGroups.ts"
      provides: "Groups data fetching"
      exports: ["useGroups"]
    - path: "frontend/src/hooks/useMessages.ts"
      provides: "Messages data fetching"
      exports: ["useMessages"]
    - path: "frontend/src/hooks/useRuns.ts"
      provides: "Runs data fetching"
      exports: ["useRuns"]
  key_links:
    - from: "frontend/src/components/ui/data-table.tsx"
      to: "@tanstack/react-table"
      via: "import"
      pattern: "useReactTable, getCoreRowModel"
    - from: "frontend/src/hooks/useGroups.ts"
      to: "/api/admin/groups"
      via: "fetch"
      pattern: "fetch.*api/admin/groups"
---

<objective>
Implement shadcn-admin DataTable component with TanStack Table integration.

Purpose: Create a reusable, feature-rich data table component that replaces the existing custom implementation (per D-05). Support sorting, pagination, and loading states.
Output: Complete DataTable component system ready for Groups, Messages, and Runs pages.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/hooks/useGroups.ts
@C:/workspace/feishu-kanban/frontend/src/types/dashboard.ts

## Key Decisions (MUST IMPLEMENT)
- D-05: Use shadcn-admin DataTable based on TanStack Table
- Keep existing API hooks (useGroups, useMessages, useRuns)
- Replace only the table UI components

## Existing Hooks to Preserve
- useGroups.ts - fetches from /api/admin/groups
- useMessages.ts - fetches from /api/admin/messages
- useRuns.ts - fetches from /api/admin/runs

## Data Types (from types/dashboard.ts)
- GroupListItem: chatId, name, memberCount, status, createdAt
- MessageListItem: id, feishuMessageId, senderName, rawText, receivedAt
- AgentRun: id, chatId, status, skillName, intent, createdAt

## shadcn/ui Components Available
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow
- Button, Select
- Skeleton (for loading)
- ChevronLeft, ChevronRight icons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataTable Core Component</name>
  <files>
    - frontend/src/components/ui/data-table.tsx
  </files>
  <action>
    1. Create data-table.tsx as main DataTable component (per D-05)
    2. Based on shadcn-admin pattern with TanStack Table:
       - Props: columns, data, loading?, error?, emptyMessage?, emptyBody?
       - Use useReactTable hook with:
         * getCoreRowModel()
         * getPaginationRowModel()
         * getSortedRowModel()
       - Render with shadcn/ui Table components
       - Support sorting via column headers
       - Show loading skeleton when loading=true
       - Show empty state when no data
    3. Export DataTable component
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/ui/data-table.tsx && echo "data-table.tsx exists" || exit 1
      grep -q "useReactTable" frontend/src/components/ui/data-table.tsx && echo "TanStack Table used" || exit 1
      grep -q "Table" frontend/src/components/ui/data-table.tsx && echo "shadcn Table used" || exit 1
      grep -q "ColumnDef" frontend/src/components/ui/data-table.tsx && echo "ColumnDef type used" || exit 1
    </automated>
  </verify>
  <done>
    - data-table.tsx created with TanStack Table integration
    - Supports sorting, pagination
    - Loading and empty states handled
    - Type-safe with generics
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DataTablePagination Component</name>
  <files>
    - frontend/src/components/ui/data-table-pagination.tsx
  </files>
  <action>
    1. Create data-table-pagination.tsx with:
       - Props: table (TableInstance), totalRows
       - Previous/Next buttons with ChevronLeft/ChevronRight icons
       - Page size selector (10, 25, 50, 100)
       - Shows "Page X of Y" text
       - Shows "Showing X-Y of Z results"
    2. Use shadcn/ui Button and Select components
    3. Disable buttons when at first/last page
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/ui/data-table-pagination.tsx && echo "data-table-pagination.tsx exists" || exit 1
      grep -q "ChevronLeft\|ChevronRight" frontend/src/components/ui/data-table-pagination.tsx && echo "Pagination icons present" || exit 1
      grep -q "Button" frontend/src/components/ui/data-table-pagination.tsx && echo "shadcn Button used" || exit 1
    </automated>
  </verify>
  <done>
    - data-table-pagination.tsx created
    - Previous/Next navigation
    - Page size selector
    - Proper disabled states
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DataTableColumnHeader Component</name>
  <files>
    - frontend/src/components/ui/data-table-column-header.tsx
  </files>
  <action>
    1. Create data-table-column-header.tsx with:
       - Props: column, title, className?
       - Renders clickable header with sort indicator
       - Shows ArrowUp/ArrowDown icon based on sort state
       - Supports aria-label for accessibility
    2. Use shadcn/ui Button for header button
    3. Handle unsorted, ascending, descending states
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/ui/data-table-column-header.tsx && echo "data-table-column-header.tsx exists" || exit 1
      grep -q "ArrowUp\|ArrowDown\|ArrowUpDown" frontend/src/components/ui/data-table-column-header.tsx && echo "Sort icons present" || exit 1
      grep -q "column.getIsSorted" frontend/src/components/ui/data-table-column-header.tsx && echo "Sort state detection" || exit 1
    </automated>
  </verify>
  <done>
    - data-table-column-header.tsx created
    - Sortable column headers with indicators
    - Accessible with aria-labels
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify Existing Hooks Compatibility</name>
  <files>
    - frontend/src/hooks/useGroups.ts
    - frontend/src/hooks/useMessages.ts
    - frontend/src/hooks/useRuns.ts
  </files>
  <action>
    1. Read existing hooks from backup and ensure compatibility
    2. Update imports if needed to use @/ path alias
    3. Verify all hooks return:
       - data: T[]
       - total: number
       - page: number
       - limit: number
       - loading: boolean
       - error: Error | null
       - refetch: () => void
       - setParams: (params) => void
    4. Ensure hooks work with DataTable pagination
  </action>
  <verify>
    <automated>
      test -f frontend/src/hooks/useGroups.ts && echo "useGroups.ts exists" || exit 1
      test -f frontend/src/hooks/useMessages.ts && echo "useMessages.ts exists" || exit 1
      test -f frontend/src/hooks/useRuns.ts && echo "useRuns.ts exists" || exit 1
      grep -q "export function useGroups" frontend/src/hooks/useGroups.ts && echo "useGroups exported" || exit 1
      grep -q "export function useMessages" frontend/src/hooks/useMessages.ts && echo "useMessages exported" || exit 1
      grep -q "export function useRuns" frontend/src/hooks/useRuns.ts && echo "useRuns exported" || exit 1
    </automated>
  </verify>
  <done>
    - All hooks restored with proper exports
    - Compatible with DataTable pagination
    - Type definitions preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Data fetching from backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | Table data | accept | Data already authorized at API layer |
| T-08-05 | Tampering | Column sorting | accept | Client-side only, no backend mutation |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] DataTable renders with sample data
- [ ] Sorting works when clicking column headers
- [ ] Pagination shows correct page numbers
- [ ] Page size selector changes row count
- [ ] Loading skeleton appears during data fetch
- [ ] Empty state shows when no data
- [ ] All hooks export correctly
</verification>

<success_criteria>
- DataTable component with TanStack Table integration
- Pagination controls working
- Sortable column headers
- Loading and empty states
- Existing hooks compatible
- Ready for page integration
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-03-SUMMARY.md`
</output>
