---
phase: 08-admin-dashboard-redesign
plan: 02
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - frontend/src/features/layout/app-shell.tsx
  - frontend/src/features/layout/sidebar.tsx
  - frontend/src/features/layout/header.tsx
  - frontend/src/features/layout/breadcrumb.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - REQ-08-05
must_haves:
  truths:
    - "Sidebar renders with navigation items and icons"
    - "Sidebar can collapse to icon-only mode"
    - "Header displays at top of content area"
    - "Breadcrumb shows current page hierarchy"
    - "Layout is responsive (mobile/desktop)"
    - "Active navigation item is highlighted"
  artifacts:
    - path: "frontend/src/features/layout/app-shell.tsx"
      provides: "Main layout wrapper"
      exports: ["AppShell"]
    - path: "frontend/src/features/layout/sidebar.tsx"
      provides: "Navigation sidebar"
      exports: ["Sidebar"]
    - path: "frontend/src/features/layout/header.tsx"
      provides: "Top header bar"
      exports: ["Header"]
    - path: "frontend/src/features/layout/breadcrumb.tsx"
      provides: "Breadcrumb navigation"
      exports: ["Breadcrumb"]
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/features/layout/app-shell.tsx"
      via: "import { AppShell }"
      pattern: "<AppShell>.*<Outlet />"
    - from: "frontend/src/features/layout/sidebar.tsx"
      to: "@/components/ui/sidebar"
      via: "import from shadcn/ui"
      pattern: "Sidebar, SidebarContent, SidebarHeader"
---

<objective>
Build the shadcn-admin layout components: Sidebar, Header, Breadcrumb, and AppShell wrapper.

Purpose: Implement the standard shadcn-admin layout pattern with collapsible sidebar, header with user menu, and breadcrumb navigation (per D-03).
Output: Complete layout system that wraps all admin pages.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/components/ui/sidebar.tsx
@C:/workspace/feishu-kanban/frontend/src/App.tsx

## Key Decisions (MUST IMPLEMENT)
- D-03: Full shadcn-admin layout (collapsible sidebar, header, breadcrumb)
- D-07: Light mode only

## Navigation Structure (from existing)
- Dashboard (/admin/dashboard)
- 群管理 (/admin/groups)
- 消息记录 (/admin/messages)
- 运行日志 (/admin/runs)
- Agent测试 (/admin/agent-testing) - NEW
- 系统设置 (/admin/settings)

## shadcn/ui Components Available
- Sidebar, SidebarContent, SidebarHeader, SidebarFooter
- SidebarMenu, SidebarMenuButton, SidebarMenuItem
- SidebarGroup, SidebarGroupLabel, SidebarGroupAction
- SidebarTrigger, SidebarRail (for collapsible)
- Breadcrumb, BreadcrumbItem, BreadcrumbLink
- Separator
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sidebar Component</name>
  <files>
    - frontend/src/features/layout/sidebar.tsx
  </files>
  <action>
    1. Create sidebar.tsx with shadcn/ui Sidebar component (per D-03)
    2. Navigation items:
       - Dashboard (LayoutDashboard icon) -> /admin/dashboard
       - 群管理 (Users icon) -> /admin/groups
       - 消息记录 (MessageSquare icon) -> /admin/messages
       - 运行日志 (Terminal icon) -> /admin/runs
       - Agent测试 (Beaker icon) -> /admin/agent-testing (NEW per special requirement)
       - 系统设置 (Settings icon) -> /admin/settings
    3. Include:
       - SidebarHeader with "Feishu Kanban" title and "管理后台" subtitle
       - Collapsible support via SidebarRail
       - Active state highlighting based on current route
       - Icon-only mode when collapsed
    4. Use Lucide React icons
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/layout/sidebar.tsx && echo "sidebar.tsx exists" || exit 1
      grep -q "Sidebar" frontend/src/features/layout/sidebar.tsx && echo "Uses shadcn Sidebar" || exit 1
      grep -q "LayoutDashboard\|Users\|MessageSquare\|Terminal\|Settings\|Beaker" frontend/src/features/layout/sidebar.tsx && echo "Navigation icons present" || exit 1
    </automated>
  </verify>
  <done>
    - sidebar.tsx created with all navigation items
    - Uses shadcn/ui Sidebar components
    - Icons from Lucide React
    - Active route highlighting implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Header Component</name>
  <files>
    - frontend/src/features/layout/header.tsx
  </files>
  <action>
    1. Create header.tsx with:
       - SidebarTrigger button (hamburger for mobile/tablet)
       - Search input placeholder (visual only for now)
       - User avatar dropdown (placeholder user)
       - Optional: notification bell icon
    2. Style with shadcn/ui components:
       - Input for search
       - Avatar for user
       - DropdownMenu for user menu
    3. Light mode only (per D-07)
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/layout/header.tsx && echo "header.tsx exists" || exit 1
      grep -q "SidebarTrigger" frontend/src/features/layout/header.tsx && echo "SidebarTrigger present" || exit 1
    </automated>
  </verify>
  <done>
    - header.tsx created with search, user menu
    - SidebarTrigger for mobile navigation
    - Uses shadcn/ui components
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Breadcrumb Component</name>
  <files>
    - frontend/src/features/layout/breadcrumb.tsx
  </files>
  <action>
    1. Create breadcrumb.tsx with:
       - Route-to-breadcrumb mapping:
         * /admin/dashboard -> ["Admin", "Dashboard"]
         * /admin/groups -> ["Admin", "群管理"]
         * /admin/messages -> ["Admin", "消息记录"]
         * /admin/runs -> ["Admin", "运行日志"]
         * /admin/agent-testing -> ["Admin", "Agent测试"]
         * /admin/settings -> ["Admin", "系统设置"]
       - Uses shadcn/ui Breadcrumb components
       - Active page shown as current (not link)
    2. Integrate with react-router useLocation
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/layout/breadcrumb.tsx && echo "breadcrumb.tsx exists" || exit 1
      grep -q "Breadcrumb" frontend/src/features/layout/breadcrumb.tsx && echo "Uses shadcn Breadcrumb" || exit 1
      grep -q "useLocation" frontend/src/features/layout/breadcrumb.tsx && echo "React Router integration" || exit 1
    </automated>
  </verify>
  <done>
    - breadcrumb.tsx created
    - Route mapping for all pages
    - Uses shadcn/ui Breadcrumb components
  </done>
</task>

<task type="auto">
  <name>Task 4: Create AppShell Layout Wrapper</name>
  <files>
    - frontend/src/features/layout/app-shell.tsx
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create app-shell.tsx combining:
       - Sidebar (left side)
       - Header (top)
       - Main content area with Outlet for routes
       - Standard shadcn-admin layout structure
    2. Structure:
       ```
       <SidebarProvider>
         <Sidebar />
         <SidebarInset>
           <Header />
           <main>
             <Outlet />
           </main>
         </SidebarInset>
       </SidebarProvider>
       ```
    3. Update App.tsx to use AppShell instead of Layout
    4. Remove old Layout.tsx references
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/layout/app-shell.tsx && echo "app-shell.tsx exists" || exit 1
      grep -q "SidebarProvider" frontend/src/features/layout/app-shell.tsx && echo "SidebarProvider used" || exit 1
      grep -q "Outlet" frontend/src/features/layout/app-shell.tsx && echo "Outlet for routes" || exit 1
      grep -q "AppShell" frontend/src/App.tsx && echo "AppShell in App.tsx" || exit 1
    </automated>
  </verify>
  <done>
    - app-shell.tsx created with complete layout
    - App.tsx updated to use AppShell
    - Layout renders correctly with sidebar, header, content
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Navigation | URL-based routing, no auth boundary yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | Header user info | accept | Placeholder user only, no real PII |
| T-08-03 | Spoofing | Active route highlighting | accept | Visual only, no security decision |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] Sidebar renders with all 6 navigation items
- [ ] Sidebar can collapse/expand
- [ ] Header shows at top of page
- [ ] Breadcrumb shows correct path for each route
- [ ] Mobile responsive (sidebar hides on mobile)
- [ ] Active navigation highlighted correctly
</verification>

<success_criteria>
- Complete shadcn-admin layout implemented
- Sidebar with collapsible navigation
- Header with search and user menu
- Breadcrumb navigation on all pages
- Responsive design (mobile/desktop)
- All routes render within layout
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-02-SUMMARY.md`
</output>
