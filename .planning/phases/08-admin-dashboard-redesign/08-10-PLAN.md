---
phase: 08-admin-dashboard-redesign
plan: 10
type: execute
wave: 6
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
  - 08-06
  - 08-07
  - 08-08
  - 08-09
files_modified:
  - frontend/src/App.tsx
  - frontend/src/main.tsx
  - frontend/index.html
  - frontend/package.json
  - frontend/src/index.css
autonomous: true
requirements:
  - REQ-08-01
  - REQ-08-08
must_haves:
  truths:
    - "All routes work correctly"
    - "Build succeeds without TypeScript errors"
    - "All pages render without console errors"
    - "Navigation works between pages"
    - "Sidebar active state reflects current route"
    - "All API endpoints are reachable"
    - "No legacy code remains"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Router configuration"
      exports: ["App"]
    - path: "frontend/build/"
      provides: "Production build"
      contains: ["index.html", "assets/"]
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/features/*/page.tsx"
      via: "Router routes"
      pattern: "createBrowserRouter"
---

<objective>
Final integration, cleanup, and build verification for the admin dashboard redesign.

Purpose: Complete the frontend rebuild by cleaning up old files, verifying all routes work, ensuring TypeScript compiles, and producing a production build.
Output: Fully functional, error-free admin dashboard ready for deployment.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md

## Key Decisions (MUST IMPLEMENT)
- D-01: Remove old frontend files
- D-06: All pages must work
- REQ-08-08: Build succeeds without TypeScript errors

## Final Checks Required
- All routes configured in App.tsx
- No console errors
- Build succeeds
- No old components remain
- All hooks preserved
- All types preserved

## Routes to Verify
- /admin/dashboard -> DashboardPage
- /admin/groups -> GroupsPage
- /admin/messages -> MessagesPage
- /admin/runs -> RunsPage
- /admin/settings -> SettingsPage
- /admin/agent-testing -> AgentTestingPage
- / -> redirect to /admin/dashboard
- /admin -> redirect to /admin/dashboard

## Files to Clean Up
- Remove old pages/ files
- Remove old components/ files (except ui/)
- Remove old hooks/ (keep preserved ones)
- Remove old utils/ (keep cn if exists)
- Remove old index.css (replaced)
- Remove old main.tsx (replaced)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean Up Old Files</name>
  <files>
    - frontend/src/pages/
    - frontend/src/components/
    - frontend/src/hooks/
    - frontend/src/types/
    - frontend/src/utils/
  </files>
  <action>
    1. Remove old files that have been replaced:
       - Delete old pages/ files (keep feature-based structure)
       - Delete old components/ (except components/ui/)
       - Delete old hooks/ (except preserved hooks)
       - Delete old utils/ (except lib/utils.ts)
    2. Keep these (preserved business logic):
       - src/hooks/useApi.ts
       - src/hooks/useGroups.ts
       - src/hooks/useMessages.ts
       - src/hooks/useRuns.ts
       - src/hooks/useLogPoll.ts
       - src/types/dashboard.ts
       - src/types/group-config.ts
       - src/types/admin.ts
       - src/lib/utils.ts (shadcn)
    3. Remove backup directory after verification
  </action>
  <verify>
    <automated>
      test -d frontend/src/components/ui && echo "components/ui/ preserved" || exit 1
      test -f frontend/src/hooks/useApi.ts && echo "useApi.ts preserved" || exit 1
      test -f frontend/src/types/dashboard.ts && echo "types preserved" || exit 1
      ls frontend/src/components/ | grep -v ui | wc -l | grep -q "^0" && echo "Old components cleaned" || exit 1
    </automated>
  </verify>
  <done>
    - Old files removed
    - Business logic preserved
    - Only new structure remains
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize App.tsx Router</name>
  <files>
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create final App.tsx with:
       - React Router v7 configuration
       - All routes:
         * /admin/dashboard -> DashboardPage
         * /admin/groups -> GroupsPage
         * /admin/messages -> MessagesPage
         * /admin/runs -> RunsPage
         * /admin/settings -> SettingsPage
         * /admin/agent-testing -> AgentTestingPage
         * /admin -> redirect to /admin/dashboard
         * / -> redirect to /admin/dashboard
         * 404 page
       - AppShell wrapper for all admin routes
       - Error boundary for route errors
    2. Lazy load pages for code splitting (optional)
    3. Ensure TypeScript types for routes
  </action>
  <verify>
    <automated>
      test -f frontend/src/App.tsx && echo "App.tsx exists" || exit 1
      grep -q "createBrowserRouter" frontend/src/App.tsx && echo "Router configured" || exit 1
      grep -q "DashboardPage" frontend/src/App.tsx && echo "Dashboard route" || exit 1
      grep -q "AgentTestingPage" frontend/src/App.tsx && echo "AgentTesting route" || exit 1
      grep -q "AppShell" frontend/src/App.tsx && echo "AppShell wrapper" || exit 1
    </automated>
  </verify>
  <done>
    - App.tsx finalized
    - All routes configured
    - AppShell wrapper applied
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Build and TypeScript</name>
  <files>
    - frontend/
  </files>
  <action>
    1. Run TypeScript check:
       - cd frontend && npx tsc --noEmit
       - Fix any TypeScript errors
    2. Run build:
       - cd frontend && npm run build
       - Verify no build errors
    3. Check output:
       - dist/ or build/ directory exists
       - Contains index.html and assets/
       - No console errors when served
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | grep -q "error TS" && exit 1 || echo "TypeScript check passed"
      test -d frontend/dist || test -d frontend/build && echo "Build directory exists" || exit 1
    </automated>
  </verify>
  <done>
    - TypeScript compiles without errors
    - Build succeeds
    - Output directory created
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify All Pages Render</name>
  <files>
    - frontend/src/features/dashboard/page.tsx
    - frontend/src/features/groups/page.tsx
    - frontend/src/features/messages/page.tsx
    - frontend/src/features/runs/page.tsx
    - frontend/src/features/settings/page.tsx
    - frontend/src/features/agent-testing/page.tsx
  </files>
  <action>
    1. Verify each page file exists and exports correctly
    2. Check that each page:
       - Imports from @/ paths
       - Uses shadcn/ui components
       - Has proper TypeScript types
       - No TODO or placeholder content
    3. Verify feature directory structure:
       - features/dashboard/
       - features/groups/
       - features/messages/
       - features/runs/
       - features/settings/
       - features/agent-testing/
       - features/layout/
    4. Verify all imports resolve
  </action>
  <verify>
    <automated>
      for page in dashboard groups messages runs settings agent-testing; do
        test -f frontend/src/features/$page/page.tsx || exit 1
        grep -q "export function" frontend/src/features/$page/page.tsx || exit 1
      done
      echo "All pages exist and export"
    </automated>
  </verify>
  <done>
    - All 6 pages exist
    - All export correctly
    - Imports resolve
  </done>
</task>

<task type="auto">
  <name>Task 5: Remove Backup Directory</name>
  <files>
    - frontend_backup_*/
  </files>
  <action>
    1. After successful verification, remove backup directory
    2. Keep only if explicitly requested
    3. Document what was preserved in SUMMARY
  </action>
  <verify>
    <automated>
      test ! -d frontend_backup_* && echo "Backup removed" || echo "Backup still exists (ok)"
    </automated>
  </verify>
  <done>
    - Backup directory removed (optional)
    - Migration complete
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build -> Runtime | Compiled code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20 | Information Disclosure | Build artifacts | accept | No secrets in source |
| T-08-21 | Tampering | Old file cleanup | accept | Git tracks changes |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] TypeScript compiles without errors
- [ ] Build succeeds
- [ ] All routes configured
- [ ] All pages export correctly
- [ ] No old files remain
- [ ] Business logic preserved
- [ ] shadcn/ui components present
</verification>

<success_criteria>
- TypeScript compilation: 0 errors
- Build: successful
- All pages: exported
- Routes: all configured
- Old files: cleaned
- Migration: complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-10-SUMMARY.md`
</output>
