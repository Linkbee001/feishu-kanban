---
phase: 08-admin-dashboard-redesign
plan: 05
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - frontend/src/features/dashboard/page.tsx
  - frontend/src/features/dashboard/components/stats-card.tsx
  - frontend/src/features/dashboard/components/activity-list.tsx
  - frontend/src/features/dashboard/components/quick-actions.tsx
autonomous: true
requirements:
  - REQ-08-04
must_haves:
  truths:
    - "Dashboard page loads at /admin/dashboard"
    - "Stats cards show total groups, active sessions, today messages"
    - "Activity list displays recent system events"
    - "Quick actions provide navigation shortcuts"
    - "Refresh button reloads all data"
  artifacts:
    - path: "frontend/src/features/dashboard/page.tsx"
      provides: "Dashboard page"
      exports: ["DashboardPage"]
      min_lines: 100
    - path: "frontend/src/features/dashboard/components/stats-card.tsx"
      provides: "Stats display card"
      exports: ["StatsCard"]
    - path: "frontend/src/features/dashboard/components/activity-list.tsx"
      provides: "Activity feed"
      exports: ["ActivityList"]
    - path: "frontend/src/features/dashboard/components/quick-actions.tsx"
      provides: "Quick navigation"
      exports: ["QuickActions"]
  key_links:
    - from: "frontend/src/features/dashboard/page.tsx"
      to: "/api/admin/dashboard/stats"
      via: "useApi hook"
      pattern: "useApi.*dashboard/stats"
    - from: "frontend/src/features/dashboard/page.tsx"
      to: "/api/admin/dashboard/activity"
      via: "useApi hook"
      pattern: "useApi.*dashboard/activity"
---

<objective>
Rebuild the Dashboard page with shadcn-admin components and modern styling.

Purpose: Reimplement the Dashboard page (per D-06) with shadcn/ui Card, Badge, Button components while preserving data fetching logic.
Output: Modern Dashboard page with stats cards, activity feed, and quick actions.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/hooks/useApi.ts

## Key Decisions (MUST IMPLEMENT)
- D-06: Rebuild Dashboard page with shadcn-admin components
- Keep existing useApi hook for data fetching (per RESEARCH.md)

## Existing API Endpoints
- GET /api/admin/dashboard/stats -> DashboardStats
- GET /api/admin/dashboard/activity?limit=10 -> ActivityItem[]

## DashboardStats Interface (from types/dashboard.ts)
- totalGroups: number
- activeSessions: number
- pendingConfig: number
- todayMessages: number
- totalRuns: number

## ActivityItem Interface
- id: string
- type: 'config_update' | 'task_complete' | 'group_bind' | 'message_received'
- title: string
- description: string
- timestamp: string
- link?: string

## Quick Actions
- Configure new group -> /admin/groups
- View messages -> /admin/messages
- View runs -> /admin/runs

## shadcn/ui Components
- Card, CardHeader, CardTitle, CardDescription, CardContent
- Badge, Button
- Skeleton for loading
- RefreshCw icon
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatsCard Component</name>
  <files>
    - frontend/src/features/dashboard/components/stats-card.tsx
  </files>
  <action>
    1. Create stats-card.tsx with:
       - Props: icon, title, value, subtitle?, trend?, onClick?
       - Uses shadcn/ui Card component
       - Icon from Lucide React
       - Trend shows up/down arrow with color
       - Clickable if onClick provided
    2. Layout:
       - Icon on right side, top-aligned
       - Title (label) above value
       - Large value number
       - Subtitle below with trend indicator
    3. Hover effect if clickable
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/dashboard/components/stats-card.tsx && echo "stats-card.tsx exists" || exit 1
      grep -q "Card" frontend/src/features/dashboard/components/stats-card.tsx && echo "Uses shadcn Card" || exit 1
      grep -q "icon" frontend/src/features/dashboard/components/stats-card.tsx && echo "Icon prop present" || exit 1
    </automated>
  </verify>
  <done>
    - stats-card.tsx created
    - Uses shadcn/ui Card
    - Supports icon, title, value, trend
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ActivityList Component</name>
  <files>
    - frontend/src/features/dashboard/components/activity-list.tsx
  </files>
  <action>
    1. Create activity-list.tsx with:
       - Props: activities: ActivityItem[], onActivityClick?, loading?
       - Uses shadcn/ui Card for container
       - Each activity shows:
         * Icon based on type (Settings, CheckCircle, Users, MessageSquare)
         * Title
         * Description (truncated)
         * Relative timestamp (刚刚, X分钟前, X小时前, X天前)
       - Clickable if link provided
       - Loading skeleton when loading=true
    2. Show max 10 items
    3. Empty state when no activities
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/dashboard/components/activity-list.tsx && echo "activity-list.tsx exists" || exit 1
      grep -q "ActivityItem" frontend/src/features/dashboard/components/activity-list.tsx && echo "ActivityItem type used" || exit 1
      grep -q "activities" frontend/src/features/dashboard/components/activity-list.tsx && echo "activities prop present" || exit 1
    </automated>
  </verify>
  <done>
    - activity-list.tsx created
    - Shows activity items with icons
    - Relative timestamp formatting
    - Empty and loading states
  </done>
</task>

<task type="auto">
  <name>Task 3: Create QuickActions Component</name>
  <files>
    - frontend/src/features/dashboard/components/quick-actions.tsx
  </files>
  <action>
    1. Create quick-actions.tsx with:
       - Props: actions: { label, icon, path, onClick }[]
       - Uses shadcn/ui Button for each action
       - Horizontal layout with gap
       - Icons from Lucide React
       - Wraps to next line on small screens
    2. Default actions:
       - 配置新群 (Plus) -> /admin/groups
       - 查看消息 (MessageSquare) -> /admin/messages
       - 查看运行日志 (Terminal) -> /admin/runs
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/dashboard/components/quick-actions.tsx && echo "quick-actions.tsx exists" || exit 1
      grep -q "Button" frontend/src/features/dashboard/components/quick-actions.tsx && echo "Uses shadcn Button" || exit 1
      grep -q "actions" frontend/src/features/dashboard/components/quick-actions.tsx && echo "actions prop present" || exit 1
    </automated>
  </verify>
  <done>
    - quick-actions.tsx created
    - Button-based action grid
    - Supports custom actions
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Dashboard Page</name>
  <files>
    - frontend/src/features/dashboard/page.tsx
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create page.tsx with:
       - Uses useApi hook for stats and activity data
       - Stats cards grid (3 columns on desktop)
       - Activity list card (full width)
       - Quick actions card
       - Refresh button in header
       - Loading states handled
       - Error state with retry
    2. Page structure:
       - Page header with title "Dashboard" and refresh button
       - Stats grid (3 cards)
       - Activity section (Card with ActivityList)
       - Quick actions section (Card with QuickActions)
       - Additional stats row (totalRuns, pendingConfig)
    3. Update App.tsx to point to new page location
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/dashboard/page.tsx && echo "page.tsx exists" || exit 1
      grep -q "useApi" frontend/src/features/dashboard/page.tsx && echo "useApi hook used" || exit 1
      grep -q "StatsCard" frontend/src/features/dashboard/page.tsx && echo "StatsCard used" || exit 1
      grep -q "ActivityList" frontend/src/features/dashboard/page.tsx && echo "ActivityList used" || exit 1
      grep -q "QuickActions" frontend/src/features/dashboard/page.tsx && echo "QuickActions used" || exit 1
    </automated>
  </verify>
  <done>
    - Dashboard page created
    - Uses preserved useApi hook
    - All sub-components integrated
    - App.tsx updated
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard -> API | Stats and activity data fetching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | Dashboard stats | accept | Data visible to authorized users |
| T-08-09 | Denial of Service | Refresh spam | accept | Rate limited at API level |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] Dashboard page loads at /admin/dashboard
- [ ] Stats cards show correct numbers
- [ ] Activity list displays with relative timestamps
- [ ] Quick actions navigate to correct pages
- [ ] Refresh button reloads data
- [ ] Loading skeletons appear during fetch
- [ ] All shadcn/ui components render correctly
</verification>

<success_criteria>
- Dashboard page with shadcn/ui components
- Stats cards showing system metrics
- Activity feed with recent events
- Quick navigation actions
- Refresh functionality working
- Responsive layout
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-05-SUMMARY.md`
</output>
