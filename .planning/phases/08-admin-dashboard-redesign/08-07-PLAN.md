---
phase: 08-admin-dashboard-redesign
plan: 07
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - frontend/src/features/messages/page.tsx
  - frontend/src/features/messages/components/message-list.tsx
  - frontend/src/features/messages/components/message-filters.tsx
  - frontend/src/features/messages/components/message-bubble.tsx
  - frontend/src/features/runs/page.tsx
  - frontend/src/features/runs/components/run-table.tsx
  - frontend/src/features/runs/components/log-viewer.tsx
  - frontend/src/features/runs/components/run-filters.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - REQ-08-04
must_haves:
  truths:
    - "Messages page loads at /admin/messages"
    - "Message list shows sender, content, timestamp"
    - "Message filters include group, date range, type"
    - "Runs page loads at /admin/runs"
    - "Run table shows status, skill, intent, time"
    - "Log viewer displays terminal-style logs"
    - "Pagination works on both pages"
  artifacts:
    - path: "frontend/src/features/messages/page.tsx"
      provides: "Messages page"
      exports: ["MessagesPage"]
    - path: "frontend/src/features/messages/components/message-list.tsx"
      provides: "Message list"
      exports: ["MessageList"]
    - path: "frontend/src/features/runs/page.tsx"
      provides: "Runs page"
      exports: ["RunsPage"]
    - path: "frontend/src/features/runs/components/run-table.tsx"
      provides: "Run data table"
      exports: ["RunTable"]
    - path: "frontend/src/features/runs/components/log-viewer.tsx"
      provides: "Terminal log display"
      exports: ["LogViewer"]
  key_links:
    - from: "frontend/src/features/messages/page.tsx"
      to: "frontend/src/hooks/useMessages.ts"
      via: "import { useMessages }"
    - from: "frontend/src/features/runs/page.tsx"
      to: "frontend/src/hooks/useRuns.ts"
      via: "import { useRuns }"
    - from: "frontend/src/features/runs/components/log-viewer.tsx"
      to: "frontend/src/hooks/useLogPoll.ts"
      via: "import { useLogPoll }"
---

<objective>
Rebuild the Messages and Runs pages with shadcn-admin components.

Purpose: Reimplement Messages page (message records) and Runs page (agent runs/logs) with shadcn/ui components while preserving existing hooks.
Output: Complete Messages and Runs pages with DataTable, filters, and log viewer.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/hooks/useMessages.ts
@C:/workspace/feishu-kanban/frontend/src/hooks/useRuns.ts
@C:/workspace/feishu-kanban/frontend/src/hooks/useLogPoll.ts
@C:/workspace/feishu-kanban/frontend/src/types/dashboard.ts

## Key Decisions (MUST IMPLEMENT)
- D-06: Rebuild Messages and Runs pages
- Keep existing hooks: useMessages, useRuns, useLogPoll

## Existing Hooks
- useMessages(): /api/admin/messages
- useRuns(): /api/admin/runs
- useLogPoll(runId): /api/admin/runs/{runId}/logs

## MessageListItem (from types/dashboard.ts)
- id, feishuMessageId, feishuChatId
- senderName, senderOpenId
- rawText, isBotMentioned
- receivedAt, senderType
- agentRunId?

## AgentRun (from types/dashboard.ts)
- id, chatId, chatName
- runType, status, skillName, intent
- startedAt, finishedAt, createdAt
- errorMessage?

## LogLine (from types/dashboard.ts)
- timestamp, level, message, metadata?

## Messages Page Features
- Search by sender/content
- Filter by group (Select)
- Filter by date range
- Filter by type (user/bot/all)
- Message list with sender avatar, content preview

## Runs Page Features
- Filter by status, group, date
- Run table with status badges
- Click to view logs
- Log viewer with terminal styling
- Real-time log polling

## shadcn/ui Components
- DataTable, DataTablePagination
- Input, Select, DatePicker (or native date input)
- Card, Badge, Button
- ScrollArea for log viewer
- Tabs for log/filter sections
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Messages Page Components</name>
  <files>
    - frontend/src/features/messages/components/message-filters.tsx
    - frontend/src/features/messages/components/message-bubble.tsx
    - frontend/src/features/messages/components/message-list.tsx
  </files>
  <action>
    1. Create message-filters.tsx:
       - Props: filters, onFiltersChange, onRefresh
       - Fields: search text, group select, date range, type select
       - Uses Input, Select, Button
    2. Create message-bubble.tsx:
       - Props: message: MessageListItem
       - Shows sender name, avatar (placeholder), message content
       - Timestamp, bot mention badge
       - User vs bot styling difference
    3. Create message-list.tsx:
       - Props: messages, loading, onLoadMore?
       - Scrollable list of MessageBubble
       - Uses ScrollArea
       - Empty state
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/messages/components/message-filters.tsx && echo "message-filters.tsx exists" || exit 1
      test -f frontend/src/features/messages/components/message-bubble.tsx && echo "message-bubble.tsx exists" || exit 1
      test -f frontend/src/features/messages/components/message-list.tsx && echo "message-list.tsx exists" || exit 1
      grep -q "MessageListItem" frontend/src/features/messages/components/message-bubble.tsx && echo "MessageListItem type used" || exit 1
    </automated>
  </verify>
  <done>
    - Messages components created
    - Filters with search, group, date, type
    - MessageBubble with avatar and content
    - MessageList with ScrollArea
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Messages Page</name>
  <files>
    - frontend/src/features/messages/page.tsx
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create page.tsx with:
       - Uses useMessages hook
       - State: filters (search, group, startDate, endDate, type), page, limit
       - Page header "消息记录"
       - MessageFilters component
       - MessageList component
       - Pagination controls
       - URL query params sync (for group filter from other pages)
    2. Handle group query param from /admin/groups
    3. Responsive layout
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/messages/page.tsx && echo "page.tsx exists" || exit 1
      grep -q "useMessages" frontend/src/features/messages/page.tsx && echo "useMessages hook used" || exit 1
      grep -q "MessageList" frontend/src/features/messages/page.tsx && echo "MessageList used" || exit 1
    </automated>
  </verify>
  <done>
    - Messages page created
    - URL query param support
    - Pagination working
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Runs Page Components</name>
  <files>
    - frontend/src/features/runs/components/run-filters.tsx
    - frontend/src/features/runs/components/run-table.tsx
    - frontend/src/features/runs/components/log-viewer.tsx
  </files>
  <action>
    1. Create run-filters.tsx:
       - Props: filters, onFiltersChange, onRefresh
       - Fields: status select, group select, date range
    2. Create run-table.tsx:
       - Props: runs, loading, onViewLogs
       - Columns: status (StatusBadge), chatName, skillName, intent, createdAt
       - Action: "查看日志" button
       - Uses DataTable
    3. Create log-viewer.tsx:
       - Props: runId, open, onOpenChange
       - Uses useLogPoll for real-time logs
       - Terminal styling with dark background (#0d1117)
       - Log level colors: INFO (gray), EXEC (blue), SUCCESS (green), WARN (yellow), ERROR (red)
       - Auto-scroll toggle
       - Pause/Resume
       - Uses ScrollArea
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/runs/components/run-filters.tsx && echo "run-filters.tsx exists" || exit 1
      test -f frontend/src/features/runs/components/run-table.tsx && echo "run-table.tsx exists" || exit 1
      test -f frontend/src/features/runs/components/log-viewer.tsx && echo "log-viewer.tsx exists" || exit 1
      grep -q "useLogPoll" frontend/src/features/runs/components/log-viewer.tsx && echo "useLogPoll used" || exit 1
      grep -q "StatusBadge" frontend/src/features/runs/components/run-table.tsx && echo "StatusBadge used" || exit 1
    </automated>
  </verify>
  <done>
    - Runs components created
    - RunTable with DataTable
    - LogViewer with terminal styling
    - Real-time log polling
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Runs Page</name>
  <files>
    - frontend/src/features/runs/page.tsx
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create page.tsx with:
       - Uses useRuns hook
       - State: filters, page, limit, selectedRunId
       - Page header "运行日志"
       - RunFilters component
       - RunTable component
       - LogViewer drawer (opens when viewing logs)
       - Pagination
    2. Handle view logs: set selectedRunId, open LogViewer
    3. Update App.tsx routes
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/runs/page.tsx && echo "page.tsx exists" || exit 1
      grep -q "useRuns" frontend/src/features/runs/page.tsx && echo "useRuns hook used" || exit 1
      grep -q "LogViewer" frontend/src/features/runs/page.tsx && echo "LogViewer used" || exit 1
    </automated>
  </verify>
  <done>
    - Runs page created
    - Log viewer integrated
    - Pagination working
    - App routes updated
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Messages/Runs -> API | Data fetching |
| Log Viewer -> API | Real-time log streaming |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Information Disclosure | Messages | accept | Authorized admin only |
| T-08-14 | Denial of Service | Log polling | accept | Rate limited, limited buffer |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] Messages page loads with list
- [ ] Search filters messages
- [ ] Group/date/type filters work
- [ ] Runs page loads with table
- [ ] Run status badges show correctly
- [ ] "查看日志" opens log viewer
- [ ] Log viewer shows terminal-style output
- [ ] Log levels have correct colors
- [ ] Real-time polling updates logs
</verification>

<success_criteria>
- Messages page with filters and list
- Message bubbles with proper styling
- Runs page with DataTable
- Log viewer with terminal UI
- Real-time log streaming
- Pagination on both pages
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-07-SUMMARY.md`
</output>
