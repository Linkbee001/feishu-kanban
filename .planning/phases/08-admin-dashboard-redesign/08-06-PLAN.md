---
phase: 08-admin-dashboard-redesign
plan: 06
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - frontend/src/features/groups/page.tsx
  - frontend/src/features/groups/components/group-table.tsx
  - frontend/src/features/groups/components/group-filters.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - REQ-08-04
must_haves:
  truths:
    - "Groups page loads at /admin/groups"
    - "DataTable shows group list with columns"
    - "Filters allow searching by name/status"
    - "Row actions include Configure, View Messages, Unbind"
    - "Group config drawer opens on Configure"
    - "Pagination controls work"
  artifacts:
    - path: "frontend/src/features/groups/page.tsx"
      provides: "Groups page"
      exports: ["GroupsPage"]
      min_lines: 150
    - path: "frontend/src/features/groups/components/group-table.tsx"
      provides: "Group data table"
      exports: ["GroupTable"]
    - path: "frontend/src/features/groups/components/group-filters.tsx"
      provides: "Filter controls"
      exports: ["GroupFilters"]
  key_links:
    - from: "frontend/src/features/groups/page.tsx"
      to: "frontend/src/hooks/useGroups.ts"
      via: "import { useGroups }"
    - from: "frontend/src/features/groups/page.tsx"
      to: "frontend/src/features/groups/group-config-drawer.tsx"
      via: "import { GroupConfigDrawer }"
    - from: "frontend/src/features/groups/page.tsx"
      to: "frontend/src/components/ui/data-table.tsx"
      via: "import { DataTable }"
---

<objective>
Rebuild the Groups page with shadcn-admin DataTable and Drawer components.

Purpose: Reimplement the Groups management page (per D-06) with shadcn/ui DataTable, filters, and Group Config Drawer while preserving useGroups hook.
Output: Complete Groups page with search, filtering, table, and configuration drawer.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/hooks/useGroups.ts
@C:/workspace/feishu-kanban/frontend/src/types/dashboard.ts
@C:/workspace/feishu-kanban/frontend/src/types/group-config.ts

## Key Decisions (MUST IMPLEMENT)
- D-05: Use shadcn-admin DataTable (from 08-03)
- D-06: Rebuild Groups page with shadcn/ui
- Keep existing useGroups hook

## Existing Hooks
- useGroups(): fetches from /api/admin/groups
- Returns: groups, total, page, limit, loading, error, refetch, setParams, unbind

## GroupListItem (from types/dashboard.ts)
- chatId: string
- name: string
- memberCount: number
- status: 'bound' | 'pending_config' | 'unbound'
- createdAt: string
- lastActiveAt: string

## Table Columns
- 群名称 (name) - flexible width
- Chat ID - fixed width 200px
- 成员数 - fixed width 100px
- 状态 - fixed width 120px (with StatusBadge)
- 创建时间 - fixed width 160px
- 操作 - fixed width 120px (dropdown)

## Row Actions (in dropdown)
- 配置 -> Open GroupConfigDrawer
- 查看消息 -> Navigate to /admin/messages?group={chatId}
- 解绑 -> ConfirmDialog then call unbind()

## Filters
- Search: by name or chatId
- Status: All / 已绑定 / 待配置 / 已解绑

## shadcn/ui Components
- DataTable, DataTablePagination
- Input for search
- Select for status filter
- Button for refresh
- DropdownMenu for actions
- GroupConfigDrawer (from 08-04)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GroupTable Component</name>
  <files>
    - frontend/src/features/groups/components/group-table.tsx
  </files>
  <action>
    1. Create group-table.tsx with:
       - Props: groups, loading, error, total, page, limit, onPageChange, onLimitChange, onConfigure, onViewMessages, onUnbind
       - Column definitions using ColumnDef<GroupListItem>
       - Columns:
         * name: flexRender with font-medium
         * chatId: code style with bg-muted
         * memberCount: plain text
         * status: StatusBadge component
         * createdAt: formatted date (zh-CN locale)
         * actions: DropdownMenu with 配置, 查看消息, 解绑
       - Uses DataTable and DataTablePagination
       - Empty state: "暂无群数据"
    2. StatusBadge from 08-04
    3. DropdownMenu with icons
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/groups/components/group-table.tsx && echo "group-table.tsx exists" || exit 1
      grep -q "ColumnDef" frontend/src/features/groups/components/group-table.tsx && echo "ColumnDef type used" || exit 1
      grep -q "StatusBadge" frontend/src/features/groups/components/group-table.tsx && echo "StatusBadge used" || exit 1
      grep -q "DropdownMenu" frontend/src/features/groups/components/group-table.tsx && echo "DropdownMenu used" || exit 1
    </automated>
  </verify>
  <done>
    - group-table.tsx created
    - All columns defined with proper widths
    - Actions dropdown implemented
    - Uses DataTable components
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GroupFilters Component</name>
  <files>
    - frontend/src/features/groups/components/group-filters.tsx
  </files>
  <action>
    1. Create group-filters.tsx with:
       - Props: searchQuery, onSearchChange, statusFilter, onStatusChange, onRefresh
       - Search input with Search icon
       - Status select dropdown: All / 已绑定 / 待配置 / 已解绑
       - Refresh button with RefreshCw icon
       - Reset filters button (optional)
    2. Uses shadcn/ui:
       - Input for search
       - Select for status
       - Button for refresh
    3. Layout: flex row with gap, responsive
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/groups/components/group-filters.tsx && echo "group-filters.tsx exists" || exit 1
      grep -q "Input" frontend/src/features/groups/components/group-filters.tsx && echo "Input component used" || exit 1
      grep -q "Select" frontend/src/features/groups/components/group-filters.tsx && echo "Select component used" || exit 1
      grep -q "Search" frontend/src/features/groups/components/group-filters.tsx && echo "Search icon used" || exit 1
    </automated>
  </verify>
  <done>
    - group-filters.tsx created
    - Search and status filters
    - Refresh button
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Groups Page</name>
  <files>
    - frontend/src/features/groups/page.tsx
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create page.tsx with:
       - Uses useGroups hook for data fetching
       - State: searchQuery, statusFilter, page, limit
       - URL sync for drawer state: ?drawer=group-config&chatId=xxx
       - Page header with title "群管理" and "配置新群" button
       - GroupFilters component
       - GroupTable component
       - GroupConfigDrawer for editing
       - ConfirmDialog for unbind
       - Toast notifications for success/error
    2. Handlers:
       - handleConfigure(chatId): open drawer via URL
       - handleViewMessages(chatId): navigate to messages
       - handleUnbind(chatId): confirm then unbind()
       - handleDrawerSaved(): refetch and close
    3. Update App.tsx route
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/groups/page.tsx && echo "page.tsx exists" || exit 1
      grep -q "useGroups" frontend/src/features/groups/page.tsx && echo "useGroups hook used" || exit 1
      grep -q "GroupConfigDrawer" frontend/src/features/groups/page.tsx && echo "GroupConfigDrawer used" || exit 1
      grep -q "ConfirmDialog" frontend/src/features/groups/page.tsx && echo "ConfirmDialog used" || exit 1
    </automated>
  </verify>
  <done>
    - Groups page created
    - DataTable integration
    - Filters working
    - Drawer and confirm dialogs
    - Toast notifications
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Groups Page -> API | Group list and unbind operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | Group list | accept | Authorized admin access |
| T-08-11 | Elevation of Privilege | Unbind action | accept | Backend validates permissions |
| T-08-12 | Repudiation | Unbind action | accept | Backend logs action |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] Groups page loads at /admin/groups
- [ ] Table shows group data
- [ ] Search filters by name/chatId
- [ ] Status filter works
- [ ] Configure opens drawer
- [ ] View Messages navigates
- [ ] Unbind shows confirm dialog
- [ ] Pagination works
</verification>

<success_criteria>
- Groups page with DataTable
- Search and status filters
- Configure drawer integration
- View messages navigation
- Unbind with confirmation
- Pagination controls
- Toast notifications
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-06-SUMMARY.md`
</output>
