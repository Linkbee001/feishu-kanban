---
phase: 08-admin-dashboard-redesign
plan: 09
type: execute
wave: 5
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-07
files_modified:
  - frontend/src/features/agent-testing/page.tsx
  - frontend/src/features/agent-testing/components/task-trigger.tsx
  - frontend/src/features/agent-testing/components/log-stream.tsx
  - frontend/src/features/agent-testing/components/debug-panel.tsx
  - frontend/src/features/agent-testing/components/status-monitor.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - REQ-08-07
must_haves:
  truths:
    - "Agent Testing page loads at /admin/agent-testing"
    - "Task trigger allows selecting group, skill, and intent"
    - "Trigger button starts agent task"
    - "Log stream displays real-time logs"
    - "Debug panel shows context and variables"
    - "Status monitor shows running agents"
  artifacts:
    - path: "frontend/src/features/agent-testing/page.tsx"
      provides: "Agent Testing Dashboard page"
      exports: ["AgentTestingPage"]
    - path: "frontend/src/features/agent-testing/components/task-trigger.tsx"
      provides: "Task trigger panel"
      exports: ["TaskTrigger"]
    - path: "frontend/src/features/agent-testing/components/log-stream.tsx"
      provides: "Log display"
      exports: ["LogStream"]
    - path: "frontend/src/features/agent-testing/components/debug-panel.tsx"
      provides: "Debug info panel"
      exports: ["DebugPanel"]
    - path: "frontend/src/features/agent-testing/components/status-monitor.tsx"
      provides: "Agent status display"
      exports: ["StatusMonitor"]
  key_links:
    - from: "frontend/src/features/agent-testing/page.tsx"
      to: "/api/admin/agent/trigger"
      via: "apiPost"
    - from: "frontend/src/features/agent-testing/components/log-stream.tsx"
      to: "frontend/src/hooks/useLogPoll.ts"
      via: "import { useLogPoll }"
    - from: "frontend/src/features/agent-testing/components/task-trigger.tsx"
      to: "frontend/src/hooks/useGroups.ts"
      via: "import { useGroups }"
---

<objective>
Build the Agent Testing Dashboard - a specialized interface for testing AI agents.

Purpose: Create a new Agent Testing Dashboard page (special requirement) with task triggering, real-time log streaming, and debug information panels.
Output: Complete Agent Testing Dashboard with all required components.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/hooks/useGroups.ts
@C:/workspace/feishu-kanban/frontend/src/hooks/useLogPoll.ts
@C:/workspace/feishu-kanban/frontend/src/hooks/useApi.ts

## Key Decisions (MUST IMPLEMENT)
- Special requirement: Agent Testing Dashboard
- Real-time log streaming via useLogPoll
- Task trigger via POST /api/admin/agent/trigger (assumed endpoint)

## Dashboard Layout (from RESEARCH.md)
```
┌─────────────────────────────────────────────────────────────────┐
│                    Agent Testing Dashboard                       │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┐  ┌───────────────────────────────────┐ │
│  │   Task Trigger      │  │     Status Monitor                │ │
│  │   ┌───────────────┐  │  │  ┌─────┐ ┌─────┐ ┌─────┐        │ │
│  │   │ Select Group  │  │  │  │Idle │ │Running│ │Failed│       │ │
│  │   ├───────────────┤  │  │  └─────┘ └─────┘ └─────┘        │ │
│  │   │ Select Skill  │  │  └───────────────────────────────────┘ │
│  │   ├───────────────┤  │  ┌───────────────────────────────────┐ │
│  │   │ Intent Input  │  │  │     Real-time Log Stream          │ │
│  │   ├───────────────┤  │  │  [2024-01-15 10:23:45] [INFO] ... │ │
│  │   │ [ Trigger ]   │  │  │  [2024-01-15 10:23:46] [EXEC] ... │ │
│  │   └───────────────┘  │  │  [2024-01-15 10:23:47] [SUCCESS]  │ │
│  └─────────────────────┘  │  │  ...                              │ │
│  ┌─────────────────────┐  │  └───────────────────────────────────┘ │
│  │   Configuration     │  │  ┌───────────────────────────────────┐ │
│  │   ┌───────────────┐  │  │     Debug Information             │ │
│  │   │ Key-Value     │  │  │  Context: {...}                   │ │
│  │   │ Editor        │  │  │  Variables: {...}                 │ │
│  │   └───────────────┘  │  │  State: {...}                     │ │
│  └─────────────────────┘  │  └───────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## TaskTrigger Component
- Group selector (from useGroups)
- Skill selector: chat, task, search, etc.
- Intent input: natural language description
- Trigger button with loading state
- Validation: all fields required

## LogStream Component
- Real-time logs via useLogPoll
- Auto-scroll toggle
- Log level colors: INFO (gray), EXEC (blue), SUCCESS (green), WARN (yellow), ERROR (red)
- Pause/Resume
- Clear logs button
- Terminal-style dark background

## DebugPanel Component
- Collapsible sections
- Context JSON viewer
- Variables JSON viewer
- State JSON viewer
- Copy to clipboard
- Refresh button

## StatusMonitor Component
- Count of agents: Idle, Running, Failed
- Visual badges with counts
- Click to filter logs

## API Assumptions
- POST /api/admin/agent/trigger { groupId, skill, intent }
- Returns: { runId, status }
- Logs via useLogPoll(runId)

## shadcn/ui Components
- Card, CardHeader, CardTitle, CardContent
- Select, Input, Button, Textarea
- Badge, Progress
- ScrollArea
- Collapsible
- Tabs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskTrigger Component</name>
  <files>
    - frontend/src/features/agent-testing/components/task-trigger.tsx
  </files>
  <action>
    1. Create task-trigger.tsx with:
       - Props: onTrigger, loading?
       - State: groupId, skill, intent
       - Group selector using useGroups hook
       - Skill selector: chat, task, analyze, search
       - Intent textarea for natural language
       - Trigger button with Play icon
       - Loading state during trigger
       - Validation error if fields missing
    2. Card wrapper with header "触发任务"
    3. Uses shadcn/ui Select, Textarea, Button
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/agent-testing/components/task-trigger.tsx && echo "task-trigger.tsx exists" || exit 1
      grep -q "useGroups" frontend/src/features/agent-testing/components/task-trigger.tsx && echo "useGroups used" || exit 1
      grep -q "Card" frontend/src/features/agent-testing/components/task-trigger.tsx && echo "shadcn Card used" || exit 1
      grep -q "Select" frontend/src/features/agent-testing/components/task-trigger.tsx && echo "Select used" || exit 1
    </automated>
  </verify>
  <done>
    - task-trigger.tsx created
    - Group and skill selectors
    - Intent input
    - Trigger button
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LogStream Component</name>
  <files>
    - frontend/src/features/agent-testing/components/log-stream.tsx
  </files>
  <action>
    1. Create log-stream.tsx with:
       - Props: runId?, autoScroll?, onAutoScrollChange?
       - Uses useLogPoll(runId) for logs
       - Terminal styling: dark bg (#0d1117), monospace font
       - Log line format: [timestamp] [LEVEL] message
       - Level colors:
         * INFO: gray-500
         * EXEC: blue-500
         * SUCCESS: green-500
         * WARN: yellow-500
         * ERROR: red-500
       - ScrollArea with auto-scroll
       - Pause/Resume button
       - Clear button
       - Empty state when no logs
    2. Card wrapper with header "实时日志"
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/agent-testing/components/log-stream.tsx && echo "log-stream.tsx exists" || exit 1
      grep -q "useLogPoll" frontend/src/features/agent-testing/components/log-stream.tsx && echo "useLogPoll used" || exit 1
      grep -q "ScrollArea" frontend/src/features/agent-testing/components/log-stream.tsx && echo "ScrollArea used" || exit 1
      grep -q "font-mono" frontend/src/features/agent-testing/components/log-stream.tsx && echo "Monospace font" || exit 1
    </automated>
  </verify>
  <done>
    - log-stream.tsx created
    - Terminal styling
    - Level colors
    - Auto-scroll control
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DebugPanel Component</name>
  <files>
    - frontend/src/features/agent-testing/components/debug-panel.tsx
  </files>
  <action>
    1. Create debug-panel.tsx with:
       - Props: context?, variables?, state?, onRefresh?
       - Collapsible sections for each data type
       - JSON viewer with syntax highlighting (basic)
       - Copy to clipboard button for each section
       - Refresh button to reload debug data
       - Empty state when no data
    2. Card wrapper with header "调试信息"
    3. Uses shadcn/ui Collapsible
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/agent-testing/components/debug-panel.tsx && echo "debug-panel.tsx exists" || exit 1
      grep -q "Collapsible" frontend/src/features/agent-testing/components/debug-panel.tsx && echo "Collapsible used" || exit 1
      grep -q "context\|variables\|state" frontend/src/features/agent-testing/components/debug-panel.tsx && echo "Debug props present" || exit 1
    </automated>
  </verify>
  <done>
    - debug-panel.tsx created
    - Collapsible sections
    - Copy functionality
  </done>
</task>

<task type="auto">
  <name>Task 4: Create StatusMonitor Component</name>
  <files>
    - frontend/src/features/agent-testing/components/status-monitor.tsx
  </files>
  <action>
    1. Create status-monitor.tsx with:
       - Props: idle, running, failed, onStatusClick?
       - Three status cards:
         * Idle (gray badge): count of idle agents
         * Running (blue badge): count of running agents
         * Failed (red badge): count of failed agents
       - Clickable to filter
       - Progress bars or just counts
    2. Card wrapper with header "Agent 状态"
    3. Uses shadcn/ui Badge
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/agent-testing/components/status-monitor.tsx && echo "status-monitor.tsx exists" || exit 1
      grep -q "Badge" frontend/src/features/agent-testing/components/status-monitor.tsx && echo "Badge used" || exit 1
      grep -q "idle\|running\|failed" frontend/src/features/agent-testing/components/status-monitor.tsx && echo "Status props present" || exit 1
    </automated>
  </verify>
  <done>
    - status-monitor.tsx created
    - Status counts
    - Badge indicators
  </done>
</task>

<task type="auto">
  <name>Task 5: Create Agent Testing Page</name>
  <files>
    - frontend/src/features/agent-testing/page.tsx
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create page.tsx with layout:
       - Page header "Agent 测试"
       - Two-column layout on desktop:
         * Left: TaskTrigger, StatusMonitor
         * Right: LogStream, DebugPanel
       - State: currentRunId, logs, debugData
       - Handlers:
         * handleTrigger: POST to /api/admin/agent/trigger
         * Handle response to get runId
         * Set currentRunId for LogStream
       - Responsive: stack on mobile
    2. Update App.tsx with new route
    3. Add navigation item in Sidebar
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/agent-testing/page.tsx && echo "page.tsx exists" || exit 1
      grep -q "TaskTrigger" frontend/src/features/agent-testing/page.tsx && echo "TaskTrigger used" || exit 1
      grep -q "LogStream" frontend/src/features/agent-testing/page.tsx && echo "LogStream used" || exit 1
      grep -q "DebugPanel" frontend/src/features/agent-testing/page.tsx && echo "DebugPanel used" || exit 1
      grep -q "StatusMonitor" frontend/src/features/agent-testing/page.tsx && echo "StatusMonitor used" || exit 1
      grep -q "apiPost" frontend/src/features/agent-testing/page.tsx && echo "apiPost used" || exit 1
    </automated>
  </verify>
  <done>
    - Agent Testing page created
    - All components integrated
    - API integration
    - App routes updated
    - Sidebar navigation added
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent Testing -> API | Task trigger and log streaming |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Elevation of Privilege | Task trigger | accept | Backend validates group permissions |
| T-08-18 | Information Disclosure | Debug panel | accept | Admin-only access |
| T-08-19 | Denial of Service | Log streaming | mitigate | Log rotation, buffer limits |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] Agent Testing page loads at /admin/agent-testing
- [ ] Task Trigger has group, skill, intent fields
- [ ] Trigger button sends API request
- [ ] Log Stream shows real-time logs
- [ ] Log levels have correct colors
- [ ] Debug Panel shows context/variables
- [ ] Status Monitor shows agent counts
- [ ] Navigation item in Sidebar
</verification>

<success_criteria>
- Agent Testing Dashboard page
- Task trigger with validation
- Real-time log streaming
- Terminal-style log display
- Debug panel with collapsible sections
- Status monitor
- Full integration with API
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-09-SUMMARY.md`
</output>
