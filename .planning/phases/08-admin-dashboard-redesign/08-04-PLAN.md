---
phase: 08-admin-dashboard-redesign
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/src/components/ui/status-badge.tsx
  - frontend/src/components/ui/confirm-dialog.tsx
  - frontend/src/components/ui/empty-state.tsx
  - frontend/src/components/ui/loading-skeleton.tsx
  - frontend/src/features/groups/group-config-drawer.tsx
autonomous: true
requirements:
  - REQ-08-04
must_haves:
  truths:
    - "StatusBadge shows group status with appropriate colors"
    - "ConfirmDialog confirms destructive actions"
    - "EmptyState displays when no data available"
    - "LoadingSkeleton shows during async operations"
    - "GroupConfigDrawer provides group configuration UI"
  artifacts:
    - path: "frontend/src/components/ui/status-badge.tsx"
      provides: "Status indicator badge"
      exports: ["StatusBadge"]
    - path: "frontend/src/components/ui/confirm-dialog.tsx"
      provides: "Confirmation dialog"
      exports: ["ConfirmDialog"]
    - path: "frontend/src/components/ui/empty-state.tsx"
      provides: "Empty state display"
      exports: ["EmptyState"]
    - path: "frontend/src/components/ui/loading-skeleton.tsx"
      provides: "Loading skeleton"
      exports: ["LoadingSkeleton"]
    - path: "frontend/src/features/groups/group-config-drawer.tsx"
      provides: "Group configuration drawer"
      exports: ["GroupConfigDrawer"]
  key_links:
    - from: "frontend/src/components/ui/status-badge.tsx"
      to: "@/components/ui/badge"
      via: "import { Badge }"
    - from: "frontend/src/features/groups/group-config-drawer.tsx"
      to: "@/components/ui/drawer"
      via: "import { Drawer }"
---

<objective>
Create common UI components and reusable business components for admin pages.

Purpose: Build a library of shared components (badges, dialogs, empty states, loading states) and the Group Config Drawer that will be used across multiple pages.
Output: Complete set of common UI components ready for page implementation.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/types/dashboard.ts
@C:/workspace/feishu-kanban/frontend/src/types/group-config.ts

## Key Decisions (MUST IMPLEMENT)
- D-02: All UI elements use standard shadcn components
- Custom business components wrap shadcn/ui components

## Status Types (from types/dashboard.ts)
- GroupStatus: 'bound' | 'pending_config' | 'unbound'
- Run Status: 'queued' | 'running' | 'succeeded' | 'failed' | 'canceled'

## Existing Components to Replace
- StatusBadge.tsx -> components/ui/status-badge.tsx
- ConfirmDialog.tsx -> components/ui/confirm-dialog.tsx
- GroupConfigDrawer -> features/groups/group-config-drawer.tsx

## shadcn/ui Components Available
- Badge, Dialog, Drawer, Skeleton
- AlertDialog for confirmations
- Form, Input, Select, Textarea
- Switch, Label
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusBadge Component</name>
  <files>
    - frontend/src/components/ui/status-badge.tsx
  </files>
  <action>
    1. Create status-badge.tsx with support for:
       - GroupStatus: 'bound' (green), 'pending_config' (yellow), 'unbound' (gray)
       - RunStatus: 'queued' (gray), 'running' (blue), 'succeeded' (green), 'failed' (red), 'canceled' (orange)
    2. Use shadcn/ui Badge with variant overrides
    3. Color mapping:
       - bound/succeeded: green (default badge)
       - pending_config/queued: yellow/amber (warning badge)
       - running: blue (secondary badge)
       - failed: red (destructive badge)
       - unbound/canceled: gray (outline badge)
    4. Show Chinese labels in UI
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/ui/status-badge.tsx && echo "status-badge.tsx exists" || exit 1
      grep -q "Badge" frontend/src/components/ui/status-badge.tsx && echo "Uses shadcn Badge" || exit 1
      grep -q "bound\|pending_config\|unbound" frontend/src/components/ui/status-badge.tsx && echo "Group statuses handled" || exit 1
    </automated>
  </verify>
  <done>
    - status-badge.tsx created
    - Supports all GroupStatus values
    - Supports all RunStatus values
    - Uses shadcn/ui Badge component
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ConfirmDialog Component</name>
  <files>
    - frontend/src/components/ui/confirm-dialog.tsx
  </files>
  <action>
    1. Create confirm-dialog.tsx with:
       - Props: open, onOpenChange, title, description, onConfirm, confirmText, cancelText, confirmVariant?
       - Uses shadcn/ui AlertDialog
       - confirmVariant: 'default' | 'destructive'
       - Loading state during onConfirm
    2. Pattern:
       ```
       <ConfirmDialog
         title="解绑群"
         description="确定要解绑吗？"
         onConfirm={handleUnbind}
         confirmText="解绑"
         confirmVariant="destructive"
       >
         <Button>解绑</Button>
       </ConfirmDialog>
       ```
    3. Support trigger-as-child pattern
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/ui/confirm-dialog.tsx && echo "confirm-dialog.tsx exists" || exit 1
      grep -q "AlertDialog" frontend/src/components/ui/confirm-dialog.tsx && echo "Uses shadcn AlertDialog" || exit 1
      grep -q "onConfirm" frontend/src/components/ui/confirm-dialog.tsx && echo "onConfirm prop present" || exit 1
    </automated>
  </verify>
  <done>
    - confirm-dialog.tsx created
    - Uses AlertDialog pattern
    - Supports destructive variant
    - Trigger-as-child pattern works
  </done>
</task>

<task type="auto">
  <name>Task 3: Create EmptyState Component</name>
  <files>
    - frontend/src/components/ui/empty-state.tsx
  </files>
  <action>
    1. Create empty-state.tsx with:
       - Props: icon?, title, description?, action?
       - Centered layout with icon, title, description
       - Optional action button
       - Border and subtle background
    2. Use shadcn/ui Card for container
    3. Example usage:
       ```
       <EmptyState
         icon={Inbox}
         title="暂无数据"
         description="点击按钮添加第一条记录"
         action={{ label: "添加", onClick: handleAdd }}
       />
       ```
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/ui/empty-state.tsx && echo "empty-state.tsx exists" || exit 1
      grep -q "title" frontend/src/components/ui/empty-state.tsx && echo "Title prop present" || exit 1
    </automated>
  </verify>
  <done>
    - empty-state.tsx created
    - Supports icon, title, description, action
    - Uses Card for consistent styling
  </done>
</task>

<task type="auto">
  <name>Task 4: Create LoadingSkeleton Component</name>
  <files>
    - frontend/src/components/ui/loading-skeleton.tsx
  </files>
  <action>
    1. Create loading-skeleton.tsx with preset skeleton layouts:
       - TableSkeleton: rows x columns
       - CardSkeleton: single card
       - StatsSkeleton: stats grid
       - FormSkeleton: form fields
    2. Use shadcn/ui Skeleton component
    3. Each preset as separate component or variant prop
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/ui/loading-skeleton.tsx && echo "loading-skeleton.tsx exists" || exit 1
      grep -q "Skeleton" frontend/src/components/ui/loading-skeleton.tsx && echo "Uses shadcn Skeleton" || exit 1
    </automated>
  </verify>
  <done>
    - loading-skeleton.tsx created
    - Multiple preset layouts
    - Uses shadcn/ui Skeleton
  </done>
</task>

<task type="auto">
  <name>Task 5: Create GroupConfigDrawer Component</name>
  <files>
    - frontend/src/features/groups/group-config-drawer.tsx
  </files>
  <action>
    1. Create group-config-drawer.tsx with:
       - Props: groupId, open, onOpenChange, onSaved?
       - Uses shadcn/ui Drawer (right side)
       - Form fields based on GroupConfig type:
         * AI模型选择 (Select)
         * 系统提示词 (Textarea)
         * 温度参数 (Slider or Input)
         * 最大token数 (Input)
         * 启用状态 (Switch)
       - Load existing config via useGroupConfig hook
       - Save via apiPost/apiPatch
       - Show loading state during save
       - Validation with error messages
    2. Width: 400px on desktop
    3. Header shows group name
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/groups/group-config-drawer.tsx && echo "group-config-drawer.tsx exists" || exit 1
      grep -q "Drawer" frontend/src/features/groups/group-config-drawer.tsx && echo "Uses shadcn Drawer" || exit 1
      grep -q "groupId" frontend/src/features/groups/group-config-drawer.tsx && echo "groupId prop present" || exit 1
    </automated>
  </verify>
  <done>
    - group-config-drawer.tsx created
    - Uses shadcn/ui Drawer
    - Form with all config fields
    - Save/validation logic
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Form -> API | Config updates via drawer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | Group config | accept | Config shown to authorized admin |
| T-08-07 | Repudiation | Config changes | accept | Backend logs changes |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] StatusBadge shows correct colors for each status
- [ ] ConfirmDialog triggers onConfirm callback
- [ ] EmptyState renders with icon, title, action
- [ ] LoadingSkeleton shows pulse animation
- [ ] GroupConfigDrawer opens/closes correctly
- [ ] Drawer form fields are editable
</verification>

<success_criteria>
- StatusBadge for all status types
- ConfirmDialog for destructive actions
- EmptyState for no-data scenarios
- LoadingSkeleton for async states
- GroupConfigDrawer for editing config
- All using shadcn/ui components
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-04-SUMMARY.md`
</output>
