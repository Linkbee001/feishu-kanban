---
phase: 08-admin-dashboard-redesign
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/components.json
  - frontend/src/index.css
  - frontend/tsconfig.json
  - frontend/vite.config.ts
autonomous: true
requirements:
  - REQ-08-01
  - REQ-08-02
  - REQ-08-03
user_setup:
  - task: "Ensure Node.js 18+ is installed"
    why: "shadcn/ui CLI requires Node 18+"
must_haves:
  truths:
    - "shadcn/ui CLI is initialized with components.json"
    - "Tailwind CSS v4 is configured with @theme directive"
    - "Project has standard shadcn/ui project structure"
    - "All required shadcn/ui components are installed"
  artifacts:
    - path: "frontend/components.json"
      provides: "shadcn/ui configuration"
      contains: "style, baseColor, tsx true"
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 CSS-first config"
      contains: "@import 'tailwindcss', @theme inline"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() utility function"
      contains: "export function cn"
    - path: "frontend/src/components/ui/"
      provides: "shadcn/ui components directory"
      min_items: 30
  key_links:
    - from: "frontend/package.json"
      to: "shadcn/ui dependencies"
      via: "npx shadcn add"
      pattern: "@radix-ui/, class-variance-authority, clsx, tailwind-merge"
---

<objective>
Initialize shadcn/ui project and install all required components.

Purpose: Establish the foundation for the new admin dashboard with standard shadcn/ui component library and Tailwind CSS v4 configuration.
Output: Fully configured shadcn/ui project with all components installed and ready for use.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/package.json

## Key Decisions (MUST IMPLEMENT)
- D-01: Complete rewrite - replace entire frontend directory (per D-01)
- D-02: Full shadcn/ui component library installation (per D-02)
- D-04: Standard Tailwind CSS with CSS variables (per D-04)
- D-07: Light mode only (per D-07)

## Required shadcn/ui Components (from RESEARCH.md)
Layout: sidebar, breadcrumb, separator, scroll-area
Data Display: table, card, badge, avatar, tooltip
Forms: button, input, select, dialog, drawer, form, label, textarea, switch, checkbox, radio-group
Feedback: toast/sonner, alert, alert-dialog, skeleton, progress
Navigation: tabs, collapsible, dropdown-menu
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup and Clean Existing Frontend</name>
  <files>
    - frontend_backup_$(date +%Y%m%d)/
    - frontend/
  </files>
  <read_first>
    - C:/workspace/feishu-kanban/frontend/src/hooks/useApi.ts
    - C:/workspace/feishu-kanban/frontend/src/hooks/useGroups.ts
    - C:/workspace/feishu-kanban/frontend/src/hooks/useMessages.ts
    - C:/workspace/feishu-kanban/frontend/src/hooks/useRuns.ts
    - C:/workspace/feishu-kanban/frontend/src/hooks/useLogPoll.ts
    - C:/workspace/feishu-kanban/frontend/src/types/dashboard.ts
    - C:/workspace/feishu-kanban/frontend/src/types/group-config.ts
    - C:/workspace/feishu-kanban/frontend/src/types/admin.ts
  </read_first>
  <action>
    1. Backup existing frontend to frontend_backup_$(date +%Y%m%d)/
    2. Create new frontend directory structure
    3. Preserve business logic files in a temporary location:
       - hooks/useApi.ts (with apiPost, apiPatch, apiDelete)
       - hooks/useGroups.ts
       - hooks/useMessages.ts
       - hooks/useRuns.ts
       - hooks/useLogPoll.ts
       - types/dashboard.ts
       - types/group-config.ts
       - types/admin.ts
    4. These will be restored after shadcn/ui init
  </action>
  <verify>
    <automated>
      test -d frontend_backup_* && echo "Backup created" || exit 1
      test -d frontend && echo "Frontend dir exists" || exit 1
    </automated>
  </verify>
  <done>
    - Backup directory exists with timestamp
    - Business logic files preserved in temporary location
    - New frontend directory is clean and ready
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui Project</name>
  <files>
    - frontend/package.json
    - frontend/components.json
    - frontend/tsconfig.json
    - frontend/vite.config.ts
    - frontend/index.html
    - frontend/src/main.tsx
  </files>
  <action>
    1. Run `cd frontend && npx shadcn@latest init` with options:
       - Base color: stone (neutral, professional look)
       - TypeScript: yes
       - React: yes
       - Framework: Vite (existing)
       - CSS variables: yes
    2. Verify components.json is created with:
       - style: "new-york" (cleaner design)
       - baseColor: "stone"
       - tsx: true
       - tailwind.cssVariables: true
    3. Update vite.config.ts if needed for path aliases
  </action>
  <verify>
    <automated>
      test -f frontend/components.json && echo "components.json exists" || exit 1
      grep -q '"style":' frontend/components.json && echo "Style configured" || exit 1
      grep -q '"baseColor":' frontend/components.json && echo "Base color set" || exit 1
    </automated>
  </verify>
  <done>
    - components.json exists with proper configuration
    - shadcn/ui project structure initialized
    - Vite + React + TypeScript setup complete
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure Tailwind CSS v4</name>
  <files>
    - frontend/src/index.css
  </files>
  <action>
    1. Configure Tailwind CSS v4 with CSS-first approach (per D-04)
    2. Replace entire index.css with:
       - @import "tailwindcss"
       - @source "./src/**/*.{ts,tsx}"
       - @theme inline with custom Feishu Kanban colors:
         * --color-primary: #1d6b57 (Feishu green)
         * --color-warning: #aa5a22
         * --color-danger: #9a2f2f
         * --color-panel: #f5efe6
         * --color-ink: #17212a
         * --color-muted: #6b7780
       - Standard shadcn/ui theme variables in HSL format
    3. Remove all manual utility classes (per D-04)
    4. Light mode only (per D-07) - no dark mode variables
  </action>
  <verify>
    <automated>
      grep -q "@import \"tailwindcss\"" frontend/src/index.css && echo "Tailwind import present" || exit 1
      grep -q "@theme inline" frontend/src/index.css && echo "Theme directive present" || exit 1
      grep -q "--color-primary" frontend/src/index.css && echo "Primary color defined" || exit 1
    </automated>
  </verify>
  <done>
    - index.css uses Tailwind v4 CSS-first configuration
    - Feishu Kanban brand colors defined as CSS variables
    - Standard shadcn/ui theme variables configured
    - No manual utility classes remain
  </done>
</task>

<task type="auto">
  <name>Task 4: Install All Required shadcn/ui Components</name>
  <files>
    - frontend/src/components/ui/*.tsx
  </files>
  <action>
    1. Install all required components in single command:
       npx shadcn add sidebar breadcrumb separator scroll-area table card badge avatar tooltip button input select dialog drawer form label textarea switch checkbox radio-group sonner alert alert-dialog skeleton progress tabs collapsible dropdown-menu
    2. Verify lib/utils.ts exists with cn() function
    3. Verify components/ui/ directory has 30+ component files
  </action>
  <verify>
    <automated>
      test -f frontend/src/lib/utils.ts && echo "utils.ts exists" || exit 1
      grep -q "export function cn" frontend/src/lib/utils.ts && echo "cn() function exists" || exit 1
      test -d frontend/src/components/ui && echo "ui directory exists" || exit 1
      ls frontend/src/components/ui/*.tsx 2>/dev/null | wc -l | grep -q "30" && echo "30+ components installed"
    </automated>
  </verify>
  <done>
    - All 30+ shadcn/ui components installed
    - lib/utils.ts with cn() function exists
    - Each component in components/ui/*.tsx
  </done>
</task>

<task type="auto">
  <name>Task 5: Restore Preserved Business Logic</name>
  <files>
    - frontend/src/hooks/useApi.ts
    - frontend/src/hooks/useGroups.ts
    - frontend/src/hooks/useMessages.ts
    - frontend/src/hooks/useRuns.ts
    - frontend/src/hooks/useLogPoll.ts
    - frontend/src/types/dashboard.ts
    - frontend/src/types/group-config.ts
    - frontend/src/types/admin.ts
    - frontend/src/App.tsx
  </files>
  <action>
    1. Restore preserved business logic files to new structure
    2. Update import paths if necessary (e.g., @/hooks/ instead of ../hooks/)
    3. Create App.tsx with router configuration:
       - /admin/dashboard -> DashboardPage
       - /admin/groups -> GroupsPage
       - /admin/messages -> MessagesPage
       - /admin/runs -> RunsPage
       - /admin/settings -> SettingsPage
       - /admin/agent-testing -> AgentTestingPage (placeholder)
       - Redirect / to /admin/dashboard
    4. Ensure TypeScript compiles without errors
  </action>
  <verify>
    <automated>
      test -f frontend/src/hooks/useApi.ts && echo "useApi.ts restored" || exit 1
      test -f frontend/src/types/dashboard.ts && echo "types restored" || exit 1
      test -f frontend/src/App.tsx && echo "App.tsx created" || exit 1
      cd frontend && npm run build 2>&1 | grep -q "error TS" && exit 1 || echo "Build successful"
    </automated>
  </verify>
  <done>
    - All business logic hooks restored
    - Type definitions preserved
    - App.tsx with router configuration created
    - TypeScript build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time -> Runtime | Node packages installed at build, executed in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | node_modules | accept | Standard npm packages, use lockfile |
| T-08-02 | Information Disclosure | package.json scripts | accept | Standard build scripts, no secrets |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] Backup exists before any destructive operations
- [ ] shadcn/ui components.json created with correct config
- [ ] Tailwind CSS v4 CSS-first config applied
- [ ] All components installed in components/ui/
- [ ] Business logic files restored
- [ ] Build succeeds without TypeScript errors
</verification>

<success_criteria>
- shadcn/ui project initialized with components.json
- Tailwind CSS v4 configured with CSS variables
- 30+ shadcn/ui components installed and ready
- Existing API hooks and types preserved
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-01-SUMMARY.md`
</output>
