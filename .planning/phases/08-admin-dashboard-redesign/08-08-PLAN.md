---
phase: 08-admin-dashboard-redesign
plan: 08
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - frontend/src/features/settings/page.tsx
  - frontend/src/features/settings/components/settings-form.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - REQ-08-04
must_haves:
  truths:
    - "Settings page loads at /admin/settings"
    - "Settings form shows system configuration"
    - "Form fields include API keys, default settings"
    - "Save button persists changes"
    - "Validation shows error messages"
    - "Success notification on save"
  artifacts:
    - path: "frontend/src/features/settings/page.tsx"
      provides: "Settings page"
      exports: ["SettingsPage"]
    - path: "frontend/src/features/settings/components/settings-form.tsx"
      provides: "Settings form"
      exports: ["SettingsForm"]
  key_links:
    - from: "frontend/src/features/settings/page.tsx"
      to: "frontend/src/hooks/useApi.ts"
      via: "import { useApi, apiPatch }"
    - from: "frontend/src/features/settings/components/settings-form.tsx"
      to: "@/components/ui/form"
      via: "import { Form }"
---

<objective>
Rebuild the Settings page with shadcn-admin form components.

Purpose: Reimplement the Settings page (per D-06) with shadcn/ui Form, Input, Switch components for system configuration.
Output: Complete Settings page with form validation and save functionality.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-CONTEXT.md
@C:/workspace/feishu-kanban/.planning/phases/08-admin-dashboard-redesign/08-RESEARCH.md
@C:/workspace/feishu-kanban/frontend/src/hooks/useApi.ts

## Key Decisions (MUST IMPLEMENT)
- D-06: Rebuild Settings page
- Use react-hook-form with zod validation

## Settings API (assumed from useApi pattern)
- GET /api/admin/settings -> SystemSettings
- PATCH /api/admin/settings -> Update settings

## Settings Fields (typical)
- Feishu App ID
- Feishu App Secret (masked)
- Default AI Model
- Default Temperature
- Default Max Tokens
- Notification settings
- System preferences

## shadcn/ui Components
- Form, FormField, FormItem, FormLabel, FormControl, FormMessage
- Input, Textarea
- Select
- Switch
- Button
- Card, CardHeader, CardTitle, CardContent
- Sonner/Toast for notifications
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Settings Form Component</name>
  <files>
    - frontend/src/features/settings/components/settings-form.tsx
  </files>
  <action>
    1. Create settings-form.tsx with:
       - Props: settings, onSubmit, loading?
       - Uses react-hook-form with zod schema
       - Form fields:
         * Feishu App ID (Input)
         * Feishu App Secret (Password Input, show/hide toggle)
         * Default AI Model (Select: GPT-4, GPT-4o, etc.)
         * Default Temperature (Slider or Number Input 0-2)
         * Default Max Tokens (Number Input)
         * Enable Notifications (Switch)
         * Debug Mode (Switch)
       - Save button with loading state
       - Cancel button to reset
       - Validation errors on fields
    2. Use shadcn/ui Form components
    3. Group fields in Cards by category
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/settings/components/settings-form.tsx && echo "settings-form.tsx exists" || exit 1
      grep -q "Form" frontend/src/features/settings/components/settings-form.tsx && echo "shadcn Form used" || exit 1
      grep -q "zod" frontend/src/features/settings/components/settings-form.tsx && echo "zod schema used" || exit 1
      grep -q "react-hook-form" frontend/src/features/settings/components/settings-form.tsx && echo "react-hook-form used" || exit 1
    </automated>
  </verify>
  <done>
    - settings-form.tsx created
    - All form fields implemented
    - Validation with zod
    - Loading states
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Settings Page</name>
  <files>
    - frontend/src/features/settings/page.tsx
    - frontend/src/App.tsx
  </files>
  <action>
    1. Create page.tsx with:
       - Uses useApi hook to fetch settings
       - State: loading, saving, error
       - Page header "系统设置"
       - SettingsForm component
       - Save handler using apiPatch
       - Success/error toast notifications
       - Loading skeleton while fetching
    2. Update App.tsx route
    3. Responsive layout (max-width for form)
  </action>
  <verify>
    <automated>
      test -f frontend/src/features/settings/page.tsx && echo "page.tsx exists" || exit 1
      grep -q "useApi" frontend/src/features/settings/page.tsx && echo "useApi hook used" || exit 1
      grep -q "SettingsForm" frontend/src/features/settings/page.tsx && echo "SettingsForm used" || exit 1
      grep -q "apiPatch" frontend/src/features/settings/page.tsx && echo "apiPatch used" || exit 1
    </automated>
  </verify>
  <done>
    - Settings page created
    - Form integration
    - Save functionality
    - Toast notifications
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings Form -> API | Settings persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Information Disclosure | App Secret | mitigate | Password input type, masked display |
| T-08-16 | Tampering | Settings values | accept | Backend validation |
</threat_model>

<verification>
## Pre-flight Checks

- [ ] Settings page loads
- [ ] Form shows current settings
- [ ] Password fields are masked
- [ ] Validation prevents invalid values
- [ ] Save persists changes
- [ ] Success toast appears
- [ ] Form resets on cancel
</verification>

<success_criteria>
- Settings page with form
- All configuration fields
- Validation and error handling
- Save functionality
- Toast notifications
- Responsive layout
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-dashboard-redesign/08-08-SUMMARY.md`
</output>
