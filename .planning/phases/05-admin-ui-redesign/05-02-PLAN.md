---
phase: 05-admin-ui-redesign
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json (if exists)
autonomous: true
requirements: []
must_haves:
  truths:
    - "TanStack Table library installed"
    - "Radix UI Dialog libraries installed"
    - "Lucide-react icon library installed"
    - "All dependencies usable in frontend build"
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependency declarations"
      contains: "@tanstack/react-table"
    - path: "frontend/node_modules/@tanstack/react-table"
      provides: "TanStack Table library"
      min_version: "8.21.3"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/src/components/admin/*.tsx"
      via: "import from '@tanstack/react-table'"
      pattern: "import.*@tanstack"
---

<objective>
Install required frontend dependencies for Admin UI redesign.

Purpose: Ensure TanStack Table, Radix UI Dialog, and Lucide-react are available before component development.
Output: Updated frontend/package.json with all dependencies installed and verified.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-ui-redesign/05-RESEARCH.md
@frontend/package.json
</context>

<interfaces>
<!-- Current frontend dependencies -->

From frontend/package.json:
```json
{
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.4",
    "typescript": "^5.9.3",
    "vite": "^6.3.5"
  }
}
```

<!-- Required new dependencies from RESEARCH.md -->

From .planning/phases/05-admin-ui-redesign/05-RESEARCH.md (lines 86-90):
```bash
cd frontend
npm install @tanstack/react-table @radix-ui/react-dialog @radix-ui/react-alert-dialog @radix-ui/react-select lucide-react
```

Verified versions:
- @tanstack/react-table: 8.21.3
- @radix-ui/react-dialog: 1.1.15
- @radix-ui/react-alert-dialog: 1.1.15
- @radix-ui/react-select: 2.1.2
- lucide-react: latest
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Table and Radix UI dependencies</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json (current dependencies)
    - .planning/phases/05-admin-ui-redesign/05-RESEARCH.md (verified versions at lines 86-96)
  </read_first>
  <action>
Install frontend dependencies using npm:

1. Navigate to frontend directory
2. Run: npm install @tanstack/react-table @radix-ui/react-dialog @radix-ui/react-alert-dialog @radix-ui/react-select lucide-react

Expected packages (from RESEARCH.md):
- @tanstack/react-table@8.21.3 - table library for D-05, D-06, D-08
- @radix-ui/react-dialog@1.1.15 - modal component for D-03
- @radix-ui/react-alert-dialog@1.1.15 - confirmation dialog for dangerous actions
- @radix-ui/react-select@2.1.2 - status dropdown for D-07
- lucide-react@latest - icon library for refresh button, sort arrows

Do NOT add Ant Design or other full UI libraries (per RESEARCH.md recommendation).

After installation, verify package.json contains the new dependencies in "dependencies" section.
  </action>
  <verify>
    <automated>grep "@tanstack/react-table" frontend/package.json && grep "@radix-ui/react-dialog" frontend/package.json</automated>
  </verify>
  <done>
    - frontend/package.json lists @tanstack/react-table
    - frontend/package.json lists @radix-ui/react-dialog
    - frontend/package.json lists @radix-ui/react-alert-dialog
    - frontend/package.json lists @radix-ui/react-select
    - frontend/package.json lists lucide-react
    - All packages installed in node_modules (npm install succeeded)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify dependencies compile correctly</name>
  <files>frontend/src/types/admin.ts</files>
  <read_first>
    - frontend/package.json (new dependencies)
    - frontend/tsconfig.json (TypeScript config)
  </read_first>
  <action>
Create a minimal TypeScript file to verify imports work:

1. Create frontend/src/types/admin.ts (temporary verification file)
2. Add imports:
   - import { ColumnDef } from '@tanstack/react-table'
   - import * as AlertDialog from '@radix-ui/react-alert-dialog'
   - import { RefreshCw } from 'lucide-react'
3. Export a dummy type to confirm module resolution:
   - export type TestTableColumn = ColumnDef<{ id: string }>

This file will be replaced in Wave 1 with actual types, but verifies that:
- TanStack Table types resolve correctly
- Radix UI imports work
- Lucide-react icons import correctly

Run frontend build to confirm: cd frontend && npm run build (should not fail on import resolution)
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <done>
    - frontend/src/types/admin.ts exists with imports
    - TypeScript compiler resolves all imports
    - Frontend build succeeds without import errors
    - Ready for Wave 1 component development
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → frontend | External packages downloaded from npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | npm packages | mitigate | Use exact versions verified in RESEARCH.md, lock file prevents auto-upgrades |
| T-05-04 | Supply Chain | lucide-react | accept | Well-maintained open-source library, minimal security surface |
| T-05-05 | Supply Chain | Radix UI | accept | Accessible UI primitives, actively maintained by Radix team |
</threat_model>

<verification>
- [ ] frontend/package.json updated with 5 new dependencies
- [ ] @tanstack/react-table@8.21.3 listed
- [ ] @radix-ui/react-dialog@1.1.15 listed
- [ ] @radix-ui/react-alert-dialog@1.1.15 listed
- [ ] @radix-ui/react-select listed
- [ ] lucide-react listed
- [ ] frontend build succeeds with test imports
- [ ] No TypeScript errors on dependency imports
</verification>

<success_criteria>
Frontend dependencies ready:
- TanStack Table v8 installed and importable
- Radix UI Dialog/AlertDialog/Select installed and importable
- Lucide-react icons installed and importable
- Frontend build verifies module resolution
- No conflicts with existing TailwindCSS 4 setup
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-ui-redesign/05-02-SUMMARY.md`
</output>