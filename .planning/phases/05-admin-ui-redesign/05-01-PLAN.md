---
phase: 05-admin-ui-redesign
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/e2e/admin-ui-redesign.spec.ts
  - test/e2e/setup/frontend-test.fixture.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
must_haves:
  truths:
    - "E2E test file exists covering all UI behaviors (D-01 through D-08)"
    - "Test can run and report results"
    - "Test mocks API responses for consistent verification"
  artifacts:
    - path: "test/e2e/admin-ui-redesign.spec.ts"
      provides: "E2E test suite for Admin UI redesign"
      contains: "test.describe('Admin UI Redesign')"
    - path: "test/e2e/setup/frontend-test.fixture.ts"
      provides: "Mock API fixture for frontend tests"
      exports: ["mockRobotInstances", "mockAgentRuns"]
  key_links:
    - from: "test/e2e/admin-ui-redesign.spec.ts"
      to: "frontend/src/components/Dashboard.tsx"
      via: "Playwright page.goto('/admin')"
      pattern: "page.locator"
---

<objective>
Create E2E test infrastructure for Admin UI redesign before implementation begins.

Purpose: Establish test-first foundation (TDD approach) so implementation can be verified against concrete acceptance criteria.
Output: E2E test file with coverage for all 8 decisions (D-01 through D-08), plus mock fixture for API responses.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-ui-redesign/05-CONTEXT.md
@.planning/phases/05-admin-ui-redesign/05-RESEARCH.md
@test/e2e/dashboard-ui.spec.ts
@test/e2e/setup/e2e-test.fixture.ts
@frontend/src/components/Dashboard.tsx
</context>

<interfaces>
<!-- Key test patterns from existing E2E infrastructure -->

From test/e2e/dashboard-ui.spec.ts:
```typescript
test('Dashboard page loads with HTML structure', async ({ page }) => {
  await page.goto(`${BASE_URL}/admin`);
  await page.waitForLoadState('domcontentloaded');
  const title = await page.title();
  expect(title).toContain('Admin Dashboard');
});
```

From test/e2e/setup/e2e-test.fixture.ts:
```typescript
// Mock fixture pattern for API responses
export const mockRobotInstances = [
  { chatId: 'test-chat-1', projectName: 'Test Project', sessionMode: 'active', lastActiveAt: new Date().toISOString() }
];
```

From frontend/src/components/Dashboard.tsx:
```typescript
// Current dashboard structure - will be replaced
export function Dashboard() {
  const { data: instances, loading } = useApi<{ chatId: string; runtimeStatus: string | null }[]>('/api/admin/robot-instances');
  // Shows static stat cards - unusable
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create admin-ui-redesign E2E test scaffold</name>
  <files>test/e2e/admin-ui-redesign.spec.ts</files>
  <read_first>
    - test/e2e/dashboard-ui.spec.ts (existing test pattern)
    - playwright.config.ts (baseURL configuration)
    - .planning/phases/05-admin-ui-redesign/05-CONTEXT.md (decisions D-01 through D-08)
  </read_first>
  <behavior>
    - Test file exists at test/e2e/admin-ui-redesign.spec.ts
    - Test describes each decision requirement (D-01 to D-08)
    - Tests use Playwright locators for UI elements
    - Tests verify API endpoints return expected data shapes
    - All tests initially marked as .skip() (implementation not ready)
  </behavior>
  <action>
Create test/e2e/admin-ui-redesign.spec.ts with test scaffolds for all 8 decisions:

1. Import test, expect from @playwright/test
2. Define BASE_URL = process.env.BASE_URL || 'http://localhost:3000'
3. Create test.describe('Admin UI Redesign', () => {...})
4. Add skipped test cases for each decision:
   - D-01: 'Per-row action buttons display correctly' (buttons: 创建 Agent Run, 查看日志, 配置项目)
   - D-02: 'Status labels show correct colors' (queued=灰色, running=蓝色, syncing=橙色, succeeded=绿色, failed=红色)
   - D-03: 'Confirmation dialog appears for dangerous actions'
   - D-04: 'Manual refresh button fetches latest data'
   - D-05: 'Robot instance table columns display' (Chat ID, Session Mode, Project Name, Last Active)
   - D-06: 'Agent run table columns display' (Run ID, Status, Prompt, Created At)
   - D-07: 'Filter bar search and dropdown work'
   - D-08: 'Pagination and sorting controls work'

Each test scaffold should:
- Navigate to /admin
- Wait for DOM content loaded
- Use page.locator() to find expected UI elements (will fail until implementation)
- Include assertion placeholders (expect(true).toBe(true) as placeholder)

Mark all tests as test.skip() initially since implementation doesn't exist yet.
  </action>
  <verify>
    <automated>npx playwright test test/e2e/admin-ui-redesign.spec.ts --reporter=list 2>&1 | grep -c "skipped"</automated>
  </verify>
  <done>
    - File test/e2e/admin-ui-redesign.spec.ts exists
    - Contains test.describe('Admin UI Redesign')
    - Has 8+ test.skip() cases covering D-01 through D-08
    - Playwright test run shows skipped tests (no failures)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frontend test fixture for mock API responses</name>
  <files>test/e2e/setup/frontend-test.fixture.ts</files>
  <read_first>
    - test/e2e/setup/e2e-test.fixture.ts (existing fixture pattern)
    - .planning/phases/05-admin-ui-redesign/05-RESEARCH.md (data shapes from lines 384-443)
    - frontend/src/hooks/useApi.ts (API call pattern)
  </read_first>
  <action>
Create test/e2e/setup/frontend-test.fixture.ts with mock data matching API response shapes:

1. Export mockRobotInstances array with sample data:
   - chatId: string
   - projectName: string
   - sessionMode: 'bootstrap' | 'pending_config' | 'active'
   - lastActiveAt: ISO date string
   - runtimeStatus: 'queued' | 'running' | 'syncing' | 'succeeded' | 'failed' | null

2. Export mockAgentRuns array with sample data:
   - id: string
   - status: 'queued' | 'running' | 'syncing' | 'succeeded' | 'failed'
   - prompt: string
   - createdAt: ISO date string

3. Export mockApiResponses object with:
   - robotInstances: mockRobotInstances
   - agentRuns: mockAgentRuns

Include at least 3 sample instances and 5 sample runs to test pagination (D-08).

Use TypeScript types matching backend return shapes (from RESEARCH.md lines 388-443).
  </action>
  <verify>
    <automated>grep -c "export const mockRobotInstances" test/e2e/setup/frontend-test.fixture.ts</automated>
  </verify>
  <done>
    - File test/e2e/setup/frontend-test.fixture.ts exists
    - Exports mockRobotInstances array with 3+ items
    - Exports mockAgentRuns array with 5+ items
    - All items match TypeScript types from RESEARCH.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Mock API | Mock fixture provides synthetic data, no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | test fixture | accept | Mock data contains no sensitive values, test fixture not exposed in production |
| T-05-02 | Tampering | test file | accept | Test file not part of production build, no runtime impact |
</threat_model>

<verification>
- [ ] test/e2e/admin-ui-redesign.spec.ts exists
- [ ] Test file contains test.describe('Admin UI Redesign')
- [ ] 8+ test cases covering D-01 through D-08
- [ ] All tests initially skipped (test.skip)
- [ ] test/e2e/setup/frontend-test.fixture.ts exists
- [ ] Fixture exports mockRobotInstances and mockAgentRuns
- [ ] Mock data matches backend return shapes
</verification>

<success_criteria>
E2E test infrastructure established:
- Test file created with coverage for all 8 decisions
- Mock fixture provides consistent test data
- Tests can run (skipped status) without errors
- Ready for implementation phase to enable tests one-by-one
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-ui-redesign/05-01-SUMMARY.md`
</output>