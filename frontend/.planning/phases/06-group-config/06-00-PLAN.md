---
phase: 06-group-config
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/components/Layout.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - D-01
must_haves:
  truths:
    - "Sidebar navigation displays two items: 机器人实例, 群配置"
    - "Clicking navigation items switches between Dashboard and GroupConfig pages"
    - "URL route /admin/group-config renders GroupConfigPage"
  artifacts:
    - path: "frontend/src/components/Layout.tsx"
      provides: "Navigation layout with sidebar"
      contains: "navigation items"
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "Sidebar navigation component"
      exports: ["Sidebar"]
      contains: "navigation links"
    - path: "frontend/src/App.tsx"
      provides: "Route configuration"
      contains: "GroupConfig route"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/Layout.tsx"
      via: "import and render"
      pattern: "Layout"
    - from: "frontend/src/components/Layout.tsx"
      to: "frontend/src/components/Sidebar.tsx"
      via: "import and render"
      pattern: "Sidebar"
---

<objective>
Rebuild Layout component with sidebar navigation to support multiple pages (Dashboard and Group Config).

Purpose: Enable navigation between Dashboard (机器人实例) and Group Config (群配置) pages via left sidebar.
Output: Updated Layout, Sidebar, and App components with routing support.
</objective>

<execution_context>
@C:/workspace/feishu-kanban/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/feishu-kanban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-group-config/06-CONTEXT.md
@frontend/src/App.tsx
@frontend/src/components/Layout.tsx
@frontend/src/components/Sidebar.tsx
@frontend/src/components/Dashboard.tsx
</context>

<interfaces>
<!-- Current App.tsx structure -->

From frontend/src/App.tsx (lines 1-33):
```typescript
export function App() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-[#fbf7f1] to-[#f5efe6]">
      <header className="border-b border-gray-200 bg-white/90 backdrop-blur-sm">
        <div className="max-w-[1400px] mx-auto px-6 py-4">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-2xl font-bold text-ink">Admin Dashboard</h1>
              <p className="text-sm text-muted mt-1">飞书机器人实例管理后台</p>
            </div>
            <div className="flex items-center gap-4">
              <span className="text-sm text-muted">v1.3.0</span>
            </div>
          </div>
        </div>
      </header>
      <main className="max-w-[1400px] mx-auto px-6 py-6">
        <Dashboard />
      </main>
    </div>
  );
}
```

<!-- Current Sidebar.tsx structure -->

From frontend/src/components/Sidebar.tsx (lines 1-80):
```typescript
interface RobotInstance {
  robotName: string;
  chatId: string;
  projectName: string;
  sessionStatus: string;
  sessionMode: string;
  runtimeStatus: string | null;
}

export function Sidebar({ onSelectInstance }: SidebarProps) {
  const { data: instances, loading, error } = useApi<RobotInstance[]>('/api/admin/robot-instances');
  // Renders robot instance cards
}
```

<!-- Desired navigation structure -->

Navigation items:
- 机器人实例 (Dashboard) - current page
- 群配置 (Group Config) - new page

Route mapping:
- /admin -> Dashboard
- /admin/group-config -> GroupConfigPage
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Sidebar as navigation component</name>
  <files>frontend/src/components/Sidebar.tsx</files>
  <read_first>
    - frontend/src/components/Sidebar.tsx (current instance list sidebar)
    - frontend/src/index.css (TailwindCSS colors)
  </read_first>
  <behavior>
    - Sidebar displays navigation items with icons
    - Navigation items: 机器人实例, 群配置
    - Active item highlighted with primary color
    - Clicking item updates URL and active state
    - Uses React Router or URL-based navigation
  </behavior>
  <action>
Replace frontend/src/components/Sidebar.tsx with navigation sidebar:

1. Import dependencies:
   - import { LayoutDashboard, Settings } from 'lucide-react' (or similar icons)

2. Define NavigationItem interface:
   ```typescript
   interface NavigationItem {
     id: string;
     label: string;
     icon: React.ComponentType;
     path: string;
   }
   ```

3. Define navigation items array:
   ```typescript
   const navItems: NavigationItem[] = [
     { id: 'instances', label: '机器人实例', icon: LayoutDashboard, path: '/admin' },
     { id: 'group-config', label: '群配置', icon: Settings, path: '/admin/group-config' },
   ];
   ```

4. Implement Sidebar component:
   - Container: min-w-[200px] bg-white/90 border-r border-gray-200 p-4
   - Header: "Admin" or app name
   - Navigation list:
     - Each item: flex items-center gap-3 px-4 py-3 rounded-xl transition-all
     - Active state: bg-primary/10 text-primary font-medium
     - Inactive state: text-muted hover:bg-gray-100
     - Click handler: window.location.href or React Router navigation

5. Handle active state based on current URL:
   - const currentPath = window.location.pathname
   - const isActive = item.path === currentPath

6. Export Sidebar

Per D-01: "左侧导航栏 + 独立页面"
  </action>
  <verify>
    <automated>grep -c "群配置" frontend/src/components/Sidebar.tsx</automated>
  </verify>
  <done>
    - Sidebar.tsx displays 2 navigation items
    - 机器人实例 and 群配置 items visible
    - Active item visually highlighted
    - Clicking items navigates to correct path
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Layout component for navigation</name>
  <files>frontend/src/components/Layout.tsx</files>
  <read_first>
    - frontend/src/components/Layout.tsx (current layout)
    - frontend/src/components/Sidebar.tsx (updated navigation sidebar)
  </read_first>
  <action>
Update frontend/src/components/Layout.tsx to support navigation layout:

1. Import updated Sidebar:
   - import { Sidebar } from './Sidebar'

2. Update LayoutProps to include optional currentPage:
   ```typescript
   interface LayoutProps {
     children: ReactNode;
     currentPage?: 'instances' | 'group-config';
   }
   ```

3. Update layout structure:
   - Keep hero/header section
   - Main content area: grid grid-cols-[240px_1fr] gap-6
   - Left column: <Sidebar />
   - Right column: {children} with bg-white/90 border border-white/70 rounded-3xl shadow-lg

4. Remove onSelectInstance prop (moved to Dashboard internal handling)

5. Ensure responsive padding and spacing matches existing design

Per D-01: "Dashboard 布局扩展为带侧边栏导航的完整布局"
  </action>
  <verify>
    <automated>grep -c "grid-cols-\[" frontend/src/components/Layout.tsx</automated>
  </verify>
  <done>
    - Layout uses two-column grid with sidebar
    - Sidebar renders on left side
    - Children content area on right side
    - Maintains existing visual style (bg-white/90, rounded-3xl, etc.)
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure routing in App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current simple structure)
    - frontend/src/components/Layout.tsx (updated)
    - frontend/src/components/Dashboard.tsx
  </read_first>
  <action>
Update frontend/src/App.tsx to support page routing:

1. Import components:
   - import { useState, useEffect } from 'react'
   - import { Layout } from './components/Layout'
   - import { Dashboard } from './components/Dashboard'

2. Create placeholder GroupConfigPage component inline (or import if exists):
   ```typescript
   function GroupConfigPage() {
     return (
       <div className="p-6">
         <h2 className="text-xl font-semibold text-ink">群配置</h2>
         <p className="text-muted mt-2">群配置页面占位符</p>
       </div>
     );
   }
   ```

3. Implement URL-based routing:
   ```typescript
   export function App() {
     const [currentPage, setCurrentPage] = useState<'instances' | 'group-config'>('instances');

     useEffect(() => {
       const path = window.location.pathname;
       if (path === '/admin/group-config') {
         setCurrentPage('group-config');
       } else {
         setCurrentPage('instances');
       }
     }, []);

     return (
       <div className="min-h-screen bg-gradient-to-b from-[#fbf7f1] to-[#f5efe6]">
         <header className="border-b border-gray-200 bg-white/90 backdrop-blur-sm">
           <div className="max-w-[1400px] mx-auto px-6 py-4">
             <h1 className="text-2xl font-bold text-ink">Admin Dashboard</h1>
           </div>
         </header>
         <main className="max-w-[1400px] mx-auto px-6 py-6">
           <Layout>
             {currentPage === 'instances' ? <Dashboard /> : <GroupConfigPage />}
           </Layout>
         </main>
       </div>
     );
   }
   ```

4. Note: GroupConfigPage will be replaced in Wave 1

Per D-01: Route setup for /admin/group-config
  </action>
  <verify>
    <automated>grep -c "group-config" frontend/src/App.tsx</automated>
  </verify>
  <done>
    - App.tsx renders Layout component
    - Layout contains Sidebar navigation
    - URL /admin/group-config shows GroupConfigPage
    - URL /admin shows Dashboard
    - Navigation between pages works
  </done>
</task>

<task type="auto">
  <name>Task 4: Create placeholder GroupConfigPage</name>
  <files>frontend/src/components/GroupConfigPage.tsx</files>
  <read_first>
    - frontend/src/components/Dashboard.tsx (style reference)
  </read_first>
  <action>
Create frontend/src/components/GroupConfigPage.tsx as placeholder:

1. Export placeholder component:
   ```typescript
   export function GroupConfigPage() {
     return (
       <div className="p-6">
         <div className="border-b border-gray-200 pb-4">
           <h2 className="text-xl font-semibold text-ink">群项目配置</h2>
           <p className="text-sm text-muted mt-1">为飞书群配置机器人项目</p>
         </div>
         <div className="mt-6 p-8 text-center">
           <p className="text-muted">群配置表单将在此实现</p>
         </div>
       </div>
     );
   }
   ```

2. Update App.tsx to import GroupConfigPage:
   - import { GroupConfigPage } from './components/GroupConfigPage'

This file will be fully implemented in Wave 1.
  </action>
  <verify>
    <automated>test -f frontend/src/components/GroupConfigPage.tsx</automated>
  </verify>
  <done>
    - GroupConfigPage.tsx exists
    - Exports GroupConfigPage component
    - Basic structure with header
    - App.tsx imports GroupConfigPage
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Navigation | Clicking nav items triggers page change |
| URL → Component | URL path determines rendered page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | URL path | accept | Navigation paths are public, no sensitive data in URL |
| T-06-02 | Spoofing | active nav item | accept | Visual indicator only, no security decision based on CSS |
</threat_model>

<verification>
- [ ] Sidebar.tsx displays 2 navigation items
- [ ] 机器人实例 and 群配置 visible
- [ ] Active item highlighted
- [ ] Layout.tsx uses two-column grid
- [ ] App.tsx renders Layout with correct page
- [ ] URL /admin/group-config works
- [ ] Navigation between pages works
</verification>

<success_criteria>
Navigation and layout complete:
- Sidebar navigation displays 机器人实例 and 群配置
- Clicking items switches between pages
- URL /admin/group-config renders GroupConfigPage
- Dashboard and GroupConfig pages share common Layout
- Visual design consistent with existing UI
</success_criteria>

<output>
After completion, create `.planning/phases/06-group-config/06-00-SUMMARY.md`
</output>
